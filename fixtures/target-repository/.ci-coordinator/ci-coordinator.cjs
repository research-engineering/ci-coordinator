// Generated by scripts.target_control_bundle; do not edit; source-sha256=4ace53bff558b8566db18e164388c44aea94ea5041cf38300f149f215f14e872; esbuild=0.28.2.
"use strict";
var __getOwnPropNames = Object.getOwnPropertyNames;
var __commonJS = (cb, mod) => function __require() {
  try {
    return mod || (0, cb[__getOwnPropNames(cb)[0]])((mod = { exports: {} }).exports, mod), mod.exports;
  } catch (e) {
    throw mod = 0, e;
  }
};

// consume_plan.cjs
var require_consume_plan = __commonJS({
  "consume_plan.cjs"(exports2, module2) {
    "use strict";
    var fs = require("node:fs");
    var { gunzipSync } = require("node:zlib");
    var CHUNK_COUNT = 7;
    var MAX_CHUNK_CHARACTERS = 65536;
    var MAX_COMPRESSED_BYTES = 344064;
    var MAX_PLAN_BYTES = 1048576;
    var FALLBACK_REASON = /^[a-z][a-z0-9_]{0,127}$/;
    var BASE64URL = /^[A-Za-z0-9_-]+$/;
    var temporaryCounter = 0;
    var FallbackError = class extends Error {
      constructor(reason) {
        super(reason);
        this.reason = reason;
      }
    };
    function planPath() {
      const value = process.env.PLAN_PATH;
      if (typeof value !== "string" || value.length === 0 || value.length > 4096 || value.includes("\0")) {
        throw new Error("plan path is invalid");
      }
      return value;
    }
    function writePrivate(content) {
      const target = planPath();
      const temporary = `${target}.tmp-${process.pid}-${temporaryCounter}`;
      temporaryCounter += 1;
      let created = false;
      try {
        const descriptor = fs.openSync(
          temporary,
          fs.constants.O_WRONLY | fs.constants.O_CREAT | fs.constants.O_EXCL | fs.constants.O_NOFOLLOW,
          384
        );
        created = true;
        try {
          fs.writeFileSync(descriptor, content);
        } finally {
          fs.closeSync(descriptor);
        }
        fs.renameSync(temporary, target);
        created = false;
      } finally {
        if (created) {
          try {
            fs.unlinkSync(temporary);
          } catch {
          }
        }
      }
    }
    function fallbackReason() {
      const value = process.env.CI_COORDINATOR_PLAN_REASON;
      return typeof value === "string" && FALLBACK_REASON.test(value) ? value : "plan_request_unavailable";
    }
    function writeFallback(reason) {
      const admittedReason = FALLBACK_REASON.test(reason) ? reason : "plan_request_unavailable";
      writePrivate(
        `${JSON.stringify({
          schemaVersion: "dynamic-ci-plan/v0",
          fallback: true,
          reason: admittedReason
        })}
`
      );
    }
    function encodedTransport() {
      const chunks = Array.from(
        { length: CHUNK_COUNT },
        (_, index) => process.env[`CI_COORDINATOR_PLAN_CHUNK_${index}`] || ""
      );
      const firstEmpty = chunks.findIndex((value) => value.length === 0);
      const presentCount = firstEmpty === -1 ? CHUNK_COUNT : firstEmpty;
      if (presentCount === 0) {
        throw new FallbackError(fallbackReason());
      }
      if (chunks.slice(presentCount).some((value) => value.length !== 0)) {
        throw new FallbackError("plan_transport_chunks_invalid");
      }
      const present = chunks.slice(0, presentCount);
      if (present.some(
        (value) => value.length > MAX_CHUNK_CHARACTERS || !BASE64URL.test(value)
      )) {
        throw new FallbackError("plan_transport_chunks_invalid");
      }
      const encoded = present.join("");
      if (fallbackReason() !== "signed_plan_available") {
        throw new FallbackError("plan_transport_state_invalid");
      }
      return encoded;
    }
    function decodePlan() {
      const encoded = encodedTransport();
      const compressed = Buffer.from(encoded, "base64url");
      if (compressed.length === 0 || compressed.length > MAX_COMPRESSED_BYTES || compressed.toString("base64url") !== encoded) {
        throw new FallbackError("plan_transport_encoding_invalid");
      }
      let content;
      try {
        content = gunzipSync(compressed, { maxOutputLength: MAX_PLAN_BYTES });
      } catch {
        throw new FallbackError("plan_transport_compression_invalid");
      }
      if (content.length === 0 || content.length > MAX_PLAN_BYTES) {
        throw new FallbackError("plan_transport_size_invalid");
      }
      let value;
      try {
        const text = new TextDecoder("utf-8", { fatal: true }).decode(content);
        value = JSON.parse(text);
      } catch {
        throw new FallbackError("coordinator_plan_response_invalid");
      }
      if (value === null || typeof value !== "object" || Array.isArray(value)) {
        throw new FallbackError("coordinator_plan_response_invalid");
      }
      return content;
    }
    function main2() {
      try {
        writePrivate(decodePlan());
      } catch (error) {
        try {
          writeFallback(
            error instanceof FallbackError ? error.reason : "plan_request_unavailable"
          );
        } catch {
          process.exitCode = 1;
        }
      }
    }
    module2.exports = Object.freeze({ main: main2 });
  }
});

// validation_core.cjs
var require_validation_core = __commonJS({
  "validation_core.cjs"(exports2, module2) {
    "use strict";
    var { createHash, createPublicKey } = require("node:crypto");
    var fs = require("node:fs");
    var { TextDecoder: TextDecoder2 } = require("node:util");
    var MAX_CLOCK_SKEW_SECONDS = 300;
    var MAX_ENVELOPE_BYTES = 1048576;
    var MAX_GITHUB_OUTPUT_UTF16_BYTES = 786432;
    var MAX_PARALLEL = 256;
    var MAX_PROFILES = 64;
    var MAX_SERVICE_PROFILE_IDS = 16;
    var MAX_SHARDS = 256;
    var MAX_TESTS = 4096;
    var MAX_TESTS_PER_SHARD = 1e4;
    var MAX_TTL_SECONDS = 300;
    var MAX_TRUST_ROOT_BYTES = 16384;
    var MAX_WORKFLOWS = 32;
    var DEFAULT_TRUST_ROOT_PATH = ".ci-coordinator/plan-trust-root.v1.json";
    var IDENTIFIER = /^[a-z][a-z0-9._-]{0,63}$/;
    var JOB_ID = /^[A-Za-z_][A-Za-z0-9_-]{0,127}$/;
    var UTF8_DECODER = new TextDecoder2("utf-8", { fatal: true });
    var ENVELOPE_KEYS = /* @__PURE__ */ new Set([
      "algorithm",
      "expiresAt",
      "issuedAt",
      "keyId",
      "payload",
      "schemaVersion",
      "signature"
    ]);
    var PAYLOAD_KEYS = /* @__PURE__ */ new Set([
      "authenticatedRun",
      "execution",
      "fallbackReason",
      "planId",
      "productionAdmissionReceiptId",
      "repository",
      "request",
      "schemaVersion",
      "verifiedPlanId",
      "verifierVersion"
    ]);
    function finish(planValid, fallback, reason, matrices = {}, maxParallelByJob = {}, selectedJobs = []) {
      let output = outputRecord(
        planValid,
        fallback,
        reason,
        matrices,
        maxParallelByJob,
        selectedJobs
      );
      if (Buffer.byteLength(output, "utf16le") > MAX_GITHUB_OUTPUT_UTF16_BYTES) {
        output = outputRecord(false, true, "provider_output_budget_exceeded", {}, {}, []);
      }
      fs.appendFileSync(process.env.GITHUB_OUTPUT, `${output}
`);
    }
    function outputRecord(planValid, fallback, reason, matrices, maxParallelByJob, selectedJobs) {
      return [
        `plan_valid=${planValid ? "true" : "false"}`,
        `fallback=${fallback ? "true" : "false"}`,
        `reason=${oneLine(reason)}`,
        `matrices=${JSON.stringify(matrices)}`,
        `max_parallel_by_job=${JSON.stringify(maxParallelByJob)}`,
        `selected_jobs=${JSON.stringify(selectedJobs)}`
      ].join("\n");
    }
    function oneLine(value) {
      return String(value ?? "unknown").replace(/[^\x20-\x7E]/g, "_").slice(0, 200);
    }
    function failClosed(reason) {
      finish(false, true, reason);
    }
    function hasExactKeys(value, expected) {
      return value !== null && typeof value === "object" && !Array.isArray(value) && Object.keys(value).length === expected.size && Object.keys(value).every((key) => expected.has(key));
    }
    function canonicalStrings(value, { allowEmpty = false, maximum = 4096, predicate = () => true } = {}) {
      if (!Array.isArray(value) || value.length > maximum || !allowEmpty && value.length === 0) {
        return false;
      }
      if (value.some(
        (item) => typeof item !== "string" || item.length === 0 || !predicate(item)
      )) {
        return false;
      }
      const sorted = [...new Set(value)].sort(compareUtf16CodeUnits);
      return sorted.length === value.length && sorted.every((item, index) => item === value[index]);
    }
    function compareUnicodeCodePoints(left, right) {
      const leftCodePoints = Array.from(left);
      const rightCodePoints = Array.from(right);
      const length = Math.min(leftCodePoints.length, rightCodePoints.length);
      for (let index = 0; index < length; index += 1) {
        const difference = leftCodePoints[index].codePointAt(0) - rightCodePoints[index].codePointAt(0);
        if (difference !== 0) {
          return difference;
        }
      }
      return leftCodePoints.length - rightCodePoints.length;
    }
    function compareUtf16CodeUnits(left, right) {
      const length = Math.min(left.length, right.length);
      for (let index = 0; index < length; index += 1) {
        const difference = left.charCodeAt(index) - right.charCodeAt(index);
        if (difference !== 0) {
          return difference;
        }
      }
      return left.length - right.length;
    }
    function stableStringify(value) {
      if (value === null || typeof value !== "object") {
        return JSON.stringify(value);
      }
      if (Array.isArray(value)) {
        return `[${value.map((item) => stableStringify(item)).join(",")}]`;
      }
      return `{${Object.entries(value).sort(([left], [right]) => compareUnicodeCodePoints(left, right)).map(([key, item]) => `${JSON.stringify(key)}:${stableStringify(item)}`).join(",")}}`;
    }
    function digest(value) {
      return createHash("sha256").update(value).digest("hex");
    }
    var ValidationError = class extends Error {
    };
    function reject(code) {
      throw new ValidationError(code);
    }
    function readJson(path, maximumBytes) {
      const bytes = readBoundedRegularFile(path, maximumBytes);
      let text;
      try {
        text = UTF8_DECODER.decode(bytes);
      } catch {
        reject("json_utf8_invalid");
      }
      if (!Buffer.from(text, "utf8").equals(bytes)) {
        reject("json_utf8_invalid");
      }
      try {
        return { bytes, text, value: JSON.parse(text) };
      } catch {
        reject("json_parse_invalid");
      }
    }
    function readBoundedRegularFile(path, maximumBytes) {
      if (typeof path !== "string" || path.length === 0) {
        reject("json_path_invalid");
      }
      let descriptor;
      try {
        descriptor = fs.openSync(
          path,
          fs.constants.O_RDONLY | (fs.constants.O_NOFOLLOW ?? 0)
        );
        const before = fs.fstatSync(descriptor, { bigint: true });
        if (!before.isFile() || before.size < 1n || before.size > BigInt(maximumBytes)) {
          reject("json_size_invalid");
        }
        const bytes = Buffer.alloc(Number(before.size));
        let offset = 0;
        while (offset < bytes.length) {
          const count = fs.readSync(descriptor, bytes, offset, bytes.length - offset, null);
          if (count === 0) {
            reject("json_file_changed");
          }
          offset += count;
        }
        const probe = Buffer.allocUnsafe(1);
        if (fs.readSync(descriptor, probe, 0, 1, null) !== 0) {
          reject("json_file_changed");
        }
        const after = fs.fstatSync(descriptor, { bigint: true });
        if (!after.isFile() || after.dev !== before.dev || after.ino !== before.ino || after.size !== before.size || after.mtimeNs !== before.mtimeNs || after.ctimeNs !== before.ctimeNs) {
          reject("json_file_changed");
        }
        return bytes;
      } catch (error) {
        if (error instanceof ValidationError) {
          throw error;
        }
        reject("json_read_failed");
      } finally {
        if (descriptor !== void 0) {
          fs.closeSync(descriptor);
        }
      }
    }
    function readPlanTrustRoot() {
      try {
        const path = process.env.CI_COORDINATOR_PLAN_TRUST_ROOT_PATH || DEFAULT_TRUST_ROOT_PATH;
        const document = readJson(path, MAX_TRUST_ROOT_BYTES);
        const root = document.value;
        if (!hasExactKeys(
          root,
          /* @__PURE__ */ new Set(["algorithm", "keyId", "publicKeySpkiBase64", "schemaVersion"])
        ) || root.schemaVersion !== "ci-coordinator-plan-trust-root/v1" || root.algorithm !== "Ed25519" || typeof root.keyId !== "string" || !/^[A-Za-z0-9._-]{1,128}$/.test(root.keyId) || typeof root.publicKeySpkiBase64 !== "string" || root.publicKeySpkiBase64.length > 4096 || !/^[A-Za-z0-9+/]+={0,2}$/.test(root.publicKeySpkiBase64) || root.publicKeySpkiBase64.length % 4 !== 0 || document.text !== `${stableStringify(root)}
`) {
          reject("plan_trust_root_invalid");
        }
        const der = Buffer.from(root.publicKeySpkiBase64, "base64");
        if (der.toString("base64") !== root.publicKeySpkiBase64) {
          reject("plan_trust_root_invalid");
        }
        const publicKey = createPublicKey({ key: der, format: "der", type: "spki" });
        if (publicKey.type !== "public" || publicKey.asymmetricKeyType !== "ed25519") {
          reject("plan_trust_root_invalid");
        }
        return { keyId: root.keyId, publicKey };
      } catch {
        reject("plan_trust_root_invalid");
      }
    }
    module2.exports = Object.freeze({
      ENVELOPE_KEYS,
      IDENTIFIER,
      JOB_ID,
      MAX_CLOCK_SKEW_SECONDS,
      MAX_ENVELOPE_BYTES,
      MAX_PARALLEL,
      MAX_PROFILES,
      MAX_SERVICE_PROFILE_IDS,
      MAX_SHARDS,
      MAX_TESTS,
      MAX_TESTS_PER_SHARD,
      MAX_TTL_SECONDS,
      MAX_WORKFLOWS,
      PAYLOAD_KEYS,
      canonicalStrings,
      compareUtf16CodeUnits,
      digest,
      failClosed,
      finish,
      hasExactKeys,
      readJson,
      readPlanTrustRoot,
      reject,
      stableStringify
    });
  }
});

// validation_envelope.cjs
var require_validation_envelope = __commonJS({
  "validation_envelope.cjs"(exports2, module2) {
    "use strict";
    var { verify } = require("node:crypto");
    var {
      ENVELOPE_KEYS,
      MAX_CLOCK_SKEW_SECONDS,
      MAX_TTL_SECONDS,
      PAYLOAD_KEYS,
      hasExactKeys,
      stableStringify
    } = require_validation_core();
    function verifyEnvelope(envelope, publicKey, expectedKeyId) {
      if (!hasExactKeys(envelope, ENVELOPE_KEYS)) {
        return "signed_plan_envelope_shape_invalid";
      }
      if (envelope.schemaVersion !== "dynamic-ci-signed-plan-envelope/v1" || envelope.algorithm !== "Ed25519") {
        return "signed_plan_envelope_schema_unsupported";
      }
      if (envelope.keyId !== expectedKeyId) {
        return "signed_plan_key_id_mismatch";
      }
      if (!hasExactKeys(envelope.payload, PAYLOAD_KEYS)) {
        return "signed_plan_payload_shape_invalid";
      }
      if (envelope.payload.schemaVersion !== "dynamic-ci-signed-plan-payload/v2") {
        return "signed_plan_payload_schema_unsupported";
      }
      if (typeof envelope.signature !== "string" || envelope.signature.length === 0) {
        return "signed_plan_signature_missing";
      }
      if (!/^[A-Za-z0-9_-]{86}$/.test(envelope.signature)) {
        return "signed_plan_signature_invalid";
      }
      const signature = Buffer.from(envelope.signature, "base64url");
      if (signature.length !== 64 || signature.toString("base64url") !== envelope.signature) {
        return "signed_plan_signature_invalid";
      }
      const now = Date.now();
      const issuedAt = Date.parse(envelope.issuedAt);
      const expiresAt = Date.parse(envelope.expiresAt);
      if (!Number.isFinite(issuedAt) || !Number.isFinite(expiresAt)) {
        return "signed_plan_time_invalid";
      }
      if (issuedAt > now + MAX_CLOCK_SKEW_SECONDS * 1e3) {
        return "signed_plan_issued_in_future";
      }
      if (expiresAt <= now) {
        return "signed_plan_expired";
      }
      if (expiresAt <= issuedAt || expiresAt - issuedAt > MAX_TTL_SECONDS * 1e3) {
        return "signed_plan_ttl_invalid";
      }
      const unsigned = {
        schemaVersion: envelope.schemaVersion,
        keyId: envelope.keyId,
        algorithm: envelope.algorithm,
        issuedAt: envelope.issuedAt,
        expiresAt: envelope.expiresAt,
        payload: envelope.payload
      };
      try {
        const valid = verify(
          null,
          Buffer.from(stableStringify(unsigned), "utf8"),
          publicKey,
          signature
        );
        return valid ? null : "signed_plan_signature_invalid";
      } catch {
        return "signed_plan_signature_invalid";
      }
    }
    function optionalInteger(value) {
      if (!value || !/^\d+$/.test(value)) {
        return null;
      }
      const parsed = Number.parseInt(value, 10);
      return Number.isSafeInteger(parsed) ? parsed : null;
    }
    function validateBinding(payload) {
      const request = payload.request;
      const repository = payload.repository;
      if (!hasExactKeys(repository, /* @__PURE__ */ new Set(["installationId", "owner", "repository", "repositoryId"])) || !hasExactKeys(
        request,
        /* @__PURE__ */ new Set([
          "baseSha",
          "eventName",
          "executionSha",
          "headSha",
          "installationId",
          "mergeGroupHeadRef",
          "owner",
          "pullRequestNumber",
          "ref",
          "repository",
          "repositoryId",
          "requestId",
          "runAttempt",
          "schemaVersion",
          "workflowRunId"
        ])
      )) {
        return "request_binding_shape_invalid";
      }
      const installationId = optionalInteger(process.env.CI_INSTALLATION_ID);
      const repositoryId = optionalInteger(process.env.CI_REPOSITORY_ID);
      const workflowRunId = optionalInteger(process.env.CI_RUN_ID);
      const runAttempt = optionalInteger(process.env.CI_RUN_ATTEMPT);
      const pullRequestNumber = optionalInteger(process.env.CI_PULL_REQUEST_NUMBER);
      const mergeGroupHeadRef = process.env.CI_MERGE_GROUP_HEAD_REF || null;
      const expected = [
        [repository.repositoryId, repositoryId, "repository_id_mismatch"],
        [repository.owner, process.env.CI_OWNER, "repository_owner_mismatch"],
        [repository.repository, process.env.CI_REPO, "repository_name_mismatch"],
        [request.schemaVersion, "dynamic-ci-plan-request/v2", "request_schema_mismatch"],
        [request.requestId, `${repositoryId}:${workflowRunId}:${runAttempt}`, "request_id_mismatch"],
        [request.installationId, installationId, "request_installation_id_mismatch"],
        [request.repositoryId, repositoryId, "request_repository_id_mismatch"],
        [request.owner, process.env.CI_OWNER, "request_owner_mismatch"],
        [request.repository, process.env.CI_REPO, "request_repository_mismatch"],
        [request.eventName, process.env.CI_EVENT, "event_name_mismatch"],
        [request.ref, process.env.CI_REF, "ref_mismatch"],
        [request.baseSha, process.env.CI_BASE_SHA, "base_sha_mismatch"],
        [request.headSha, process.env.CI_HEAD_SHA, "head_sha_mismatch"],
        [request.executionSha, process.env.CI_EXECUTION_SHA, "execution_sha_mismatch"],
        [request.workflowRunId, workflowRunId, "workflow_run_id_mismatch"],
        [request.runAttempt, runAttempt, "run_attempt_mismatch"],
        [request.pullRequestNumber, pullRequestNumber, "pull_request_number_mismatch"],
        [request.mergeGroupHeadRef, mergeGroupHeadRef, "merge_group_head_ref_mismatch"]
      ];
      if (installationId !== null) {
        expected.push([
          repository.installationId,
          installationId,
          "repository_installation_id_mismatch"
        ]);
      }
      return expected.find(([actual, wanted]) => actual !== wanted)?.[2] ?? null;
    }
    function validateAuthenticatedRun(authenticatedRun) {
      const keys = /* @__PURE__ */ new Set([
        "audience",
        "checkRunId",
        "claimHash",
        "eventName",
        "executionSha",
        "issuer",
        "jobWorkflowRef",
        "jobWorkflowSha",
        "ref",
        "repository",
        "repositoryId",
        "runAttempt",
        "runId",
        "verifiedAt",
        "verifierVersion",
        "workflowRef",
        "workflowSha"
      ]);
      if (!hasExactKeys(authenticatedRun, keys)) {
        return "authenticated_run_shape_invalid";
      }
      const expected = [
        [authenticatedRun.issuer, "https://token.actions.githubusercontent.com", "auth_issuer_mismatch"],
        [authenticatedRun.audience, process.env.CI_COORDINATOR_PLAN_OIDC_AUDIENCE || "ci-coordinator", "auth_audience_mismatch"],
        [authenticatedRun.repository, `${process.env.CI_OWNER}/${process.env.CI_REPO}`, "auth_repository_mismatch"],
        [authenticatedRun.repositoryId, optionalInteger(process.env.CI_REPOSITORY_ID), "auth_repository_id_mismatch"],
        [authenticatedRun.ref, process.env.CI_REF, "auth_ref_mismatch"],
        [authenticatedRun.runId, optionalInteger(process.env.CI_RUN_ID), "auth_run_id_mismatch"],
        [authenticatedRun.runAttempt, optionalInteger(process.env.CI_RUN_ATTEMPT), "auth_run_attempt_mismatch"],
        [authenticatedRun.eventName, process.env.CI_EVENT, "auth_event_mismatch"],
        [authenticatedRun.executionSha, process.env.CI_EXECUTION_SHA, "auth_execution_sha_mismatch"],
        [authenticatedRun.workflowRef, process.env.CI_WORKFLOW_REF || null, "auth_workflow_ref_mismatch"],
        [authenticatedRun.workflowSha, process.env.CI_WORKFLOW_SHA || null, "auth_workflow_sha_mismatch"]
      ];
      return expected.find(([actual, wanted]) => actual !== wanted)?.[2] ?? null;
    }
    module2.exports = Object.freeze({
      validateAuthenticatedRun,
      validateBinding,
      verifyEnvelope
    });
  }
});

// validation_execution.cjs
var require_validation_execution = __commonJS({
  "validation_execution.cjs"(exports2, module2) {
    "use strict";
    var {
      IDENTIFIER,
      JOB_ID,
      MAX_PARALLEL,
      MAX_PROFILES,
      MAX_SERVICE_PROFILE_IDS,
      MAX_SHARDS,
      MAX_TESTS,
      MAX_TESTS_PER_SHARD,
      MAX_WORKFLOWS,
      canonicalStrings,
      compareUtf16CodeUnits,
      digest,
      hasExactKeys,
      readJson,
      reject,
      stableStringify
    } = require_validation_core();
    var {
      validateAuthenticatedRun,
      validateBinding
    } = require_validation_envelope();
    var EXECUTION_KINDS = /* @__PURE__ */ new Set(["native-job-set", "witness-shards"]);
    var WORKFLOW_PATH = /^\.github\/workflows\/[^/\\]+\.ya?ml$/;
    var LOCAL_REQUESTER_PATH = ".github/workflows/trusted-plan-request.yml";
    var LOCAL_REQUESTER_REF = `$/${LOCAL_REQUESTER_PATH}`;
    function loadStaticJobIds() {
      let value;
      try {
        value = JSON.parse(process.env.CI_STATIC_JOB_IDS_JSON ?? "");
      } catch {
        reject("static_job_ids_invalid");
      }
      if (!canonicalStrings(value, {
        maximum: MAX_PROFILES,
        predicate: (item) => JOB_ID.test(item)
      })) {
        reject("static_job_ids_invalid");
      }
      return new Set(value);
    }
    function loadWorkflowPath() {
      const path = process.env.CI_WORKFLOW_PATH;
      const workflowRef = process.env.CI_WORKFLOW_REF;
      const owner = process.env.CI_OWNER;
      const repository = process.env.CI_REPO;
      if (typeof path !== "string" || !WORKFLOW_PATH.test(path) || Buffer.byteLength(path, "utf8") > 256 || typeof workflowRef !== "string" || typeof owner !== "string" || typeof repository !== "string" || !workflowRef.startsWith(`${owner}/${repository}/${path}@`)) {
        reject("workflow_path_binding_invalid");
      }
      return path;
    }
    function loadRegistry(staticJobIds, workflowPath) {
      const { text: canonical, value } = readJson(
        process.env.CI_TARGET_REGISTRY_PATH,
        131072
      );
      if (!canonical.endsWith("\n") || stableStringify(value) !== canonical.slice(0, -1)) {
        throw new Error("target_registry_not_canonical");
      }
      if (!hasExactKeys(
        value,
        /* @__PURE__ */ new Set(["adapterFiles", "generator", "profiles", "schemaVersion", "workflows"])
      ) || !hasExactKeys(value.generator, /* @__PURE__ */ new Set(["id", "version"])) || !IDENTIFIER.test(value.generator.id) || !/^[0-9A-Za-z][0-9A-Za-z._-]{0,63}$/.test(value.generator.version) || value.schemaVersion !== "dynamic-ci-target-execution-registry/v1" || !validAdapterFiles(value.adapterFiles) || !Array.isArray(value.workflows) || value.workflows.length === 0 || value.workflows.length > MAX_WORKFLOWS || !Array.isArray(value.profiles) || value.profiles.length === 0 || value.profiles.length > MAX_PROFILES) {
        throw new Error("target_registry_shape_invalid");
      }
      const workflowPaths = value.workflows.map((workflow2) => workflow2?.workflowPath);
      const adapterPaths = new Set(value.adapterFiles.map((item) => item.path));
      const profileIds = value.profiles.map((profile) => profile?.profileId);
      if (!canonicalStrings(workflowPaths, {
        maximum: MAX_WORKFLOWS,
        predicate: validWorkflowPath
      }) || !canonicalStrings(profileIds, {
        maximum: MAX_PROFILES,
        predicate: (item) => IDENTIFIER.test(item)
      }) || workflowPaths.some((path) => !adapterPaths.has(path))) {
        throw new Error("target_registry_identity_invalid");
      }
      const workflows = loadWorkflows(value.workflows);
      if ([...workflows.values()].some(
        (workflow2) => workflow2.planRequestWorkflowRef === LOCAL_REQUESTER_REF
      ) && !adapterPaths.has(LOCAL_REQUESTER_PATH)) {
        throw new Error("target_registry_local_requester_missing");
      }
      const profiles = loadProfiles(value.profiles, workflows);
      for (const workflow2 of workflows.values()) {
        const actualJobs = [...profiles.values()].filter((profile) => profile.workflowPath === workflow2.workflowPath).map((profile) => profile.jobId).sort(compareUtf16CodeUnits);
        if (actualJobs.length === 0 || stableStringify(actualJobs) !== stableStringify(workflow2.executionJobIds)) {
          throw new Error("target_registry_workflow_closure_invalid");
        }
      }
      const workflow = workflows.get(workflowPath);
      if (!workflow) {
        throw new Error("target_registry_workflow_unknown");
      }
      if (workflow.executionJobIds.length !== staticJobIds.size || workflow.executionJobIds.some((jobId) => !staticJobIds.has(jobId))) {
        throw new Error("static_job_set_mismatch");
      }
      return {
        hash: digest(canonical.slice(0, -1)),
        profiles,
        workflow
      };
    }
    function validAdapterFiles(values) {
      if (!Array.isArray(values) || values.length < 2 || values.length > 33) {
        return false;
      }
      const paths = values.map((item) => item?.path);
      const controlFiles = /* @__PURE__ */ new Set([
        ".ci-coordinator/ci-coordinator.cjs"
      ]);
      return canonicalStrings(paths, {
        maximum: 33,
        predicate: (path) => validWorkflowPath(path) || controlFiles.has(path)
      }) && values.every(
        (item) => hasExactKeys(item, /* @__PURE__ */ new Set(["path", "sha256"])) && /^[0-9a-f]{64}$/.test(item.sha256)
      ) && [...controlFiles].every((path) => paths.includes(path)) && paths.some((path) => validWorkflowPath(path));
    }
    function loadWorkflows(values) {
      const keys = /* @__PURE__ */ new Set([
        "executionJobs",
        "executionKind",
        "fallbackJobId",
        "gateJobId",
        "gateSignalName",
        "planJobId",
        "planRequestJobId",
        "planRequestWorkflowRef",
        "requiredJobIds",
        "workflowPath"
      ]);
      const workflows = /* @__PURE__ */ new Map();
      for (const workflow of values) {
        const executionJobs = loadExecutionJobs(
          workflow?.executionJobs,
          workflow?.planJobId
        );
        const executionJobIds = executionJobs.map((job) => job.jobId);
        const requiredJobIds = workflow?.requiredJobIds;
        const roleJobIds = [
          workflow?.planRequestJobId,
          workflow?.planJobId,
          workflow?.gateJobId,
          ...executionJobIds,
          ...workflow?.fallbackJobId === null ? [] : [workflow?.fallbackJobId]
        ];
        if (!hasExactKeys(workflow, keys) || !validWorkflowPath(workflow.workflowPath) || !EXECUTION_KINDS.has(workflow.executionKind) || executionJobs.length === 0 || !canonicalStrings(requiredJobIds, {
          allowEmpty: true,
          maximum: MAX_PROFILES,
          predicate: (item) => JOB_ID.test(item)
        }) || requiredJobIds.some((jobId) => !executionJobIds.includes(jobId)) || !JOB_ID.test(workflow.planRequestJobId) || !validPlanRequestWorkflowRef(workflow.planRequestWorkflowRef) || !JOB_ID.test(workflow.planJobId) || !JOB_ID.test(workflow.gateJobId) || (workflow.executionKind === "witness-shards" ? !JOB_ID.test(workflow.fallbackJobId) : workflow.fallbackJobId !== null) || new Set(roleJobIds.map((jobId) => jobId.toLowerCase())).size !== roleJobIds.length || typeof workflow.gateSignalName !== "string" || workflow.gateSignalName.length === 0 || Buffer.byteLength(workflow.gateSignalName, "utf8") > 256) {
          throw new Error("target_registry_workflow_invalid");
        }
        workflows.set(workflow.workflowPath, {
          ...workflow,
          executionJobIds,
          executionJobs,
          requiredJobIds
        });
      }
      return workflows;
    }
    function loadExecutionJobs(value, planJobId) {
      if (!Array.isArray(value) || value.length === 0 || value.length > MAX_PROFILES) {
        throw new Error("target_registry_execution_jobs_invalid");
      }
      const keys = /* @__PURE__ */ new Set(["jobId", "needs"]);
      const jobIds = value.map((job) => job?.jobId);
      if (!canonicalStrings(jobIds, {
        maximum: MAX_PROFILES,
        predicate: (item) => JOB_ID.test(item)
      })) {
        throw new Error("target_registry_execution_jobs_invalid");
      }
      const known = new Set(jobIds);
      const dependencies = /* @__PURE__ */ new Map();
      for (const job of value) {
        if (!hasExactKeys(job, keys) || !canonicalStrings(job.needs, {
          allowEmpty: true,
          maximum: MAX_PROFILES,
          predicate: (item) => JOB_ID.test(item)
        }) || job.needs.includes(job.jobId) || !job.needs.includes(planJobId) || job.needs.some(
          (dependency) => dependency !== planJobId && !known.has(dependency)
        )) {
          throw new Error("target_registry_execution_jobs_invalid");
        }
        dependencies.set(
          job.jobId,
          job.needs.filter((dependency) => dependency !== planJobId)
        );
      }
      requireAcyclicJobs(dependencies);
      return value;
    }
    function requireAcyclicJobs(dependencies) {
      const visiting = /* @__PURE__ */ new Set();
      const visited = /* @__PURE__ */ new Set();
      function visit(jobId) {
        if (visiting.has(jobId)) {
          throw new Error("target_registry_execution_cycle");
        }
        if (visited.has(jobId)) {
          return;
        }
        visiting.add(jobId);
        dependencies.get(jobId).forEach(visit);
        visiting.delete(jobId);
        visited.add(jobId);
      }
      dependencies.forEach((_needs, jobId) => visit(jobId));
    }
    function loadProfiles(values, workflows) {
      const keys = /* @__PURE__ */ new Set([
        "capacityClassId",
        "credentialProfileId",
        "executionKind",
        "fixtureProfileId",
        "jobId",
        "permissionProfileId",
        "profileId",
        "runnerProfileId",
        "serviceProfileIds",
        "workflowPath"
      ]);
      const profiles = /* @__PURE__ */ new Map();
      const jobs = /* @__PURE__ */ new Set();
      for (const profile of values) {
        const workflow = workflows.get(profile?.workflowPath);
        const jobKey = `${profile?.workflowPath}\0${profile?.jobId}`;
        if (!hasExactKeys(profile, keys) || !workflow || profile.executionKind !== workflow.executionKind || !JOB_ID.test(profile.jobId) || jobs.has(jobKey) || ![
          profile.profileId,
          profile.runnerProfileId,
          profile.permissionProfileId,
          profile.credentialProfileId,
          profile.fixtureProfileId,
          profile.capacityClassId
        ].every((item) => typeof item === "string" && IDENTIFIER.test(item)) || !canonicalStrings(profile.serviceProfileIds, {
          allowEmpty: true,
          maximum: MAX_SERVICE_PROFILE_IDS,
          predicate: (item) => IDENTIFIER.test(item)
        })) {
          throw new Error("target_registry_profile_invalid");
        }
        profiles.set(profile.profileId, profile);
        jobs.add(jobKey);
      }
      return profiles;
    }
    function validWorkflowPath(value) {
      return typeof value === "string" && WORKFLOW_PATH.test(value) && Buffer.byteLength(value, "utf8") <= 256;
    }
    function validPlanRequestWorkflowRef(value) {
      if (value === LOCAL_REQUESTER_REF) return true;
      return typeof value === "string" && Buffer.byteLength(value, "utf8") <= 512 && /^[A-Za-z0-9](?:[A-Za-z0-9-]{0,37}[A-Za-z0-9])?\/[A-Za-z0-9_.-]{1,100}\/\.github\/workflows\/[^/\\]+\.ya?ml@[0-9a-f]{40}$/.test(
        value
      );
    }
    function validateProfileBinding(profile, binding) {
      const keys = /* @__PURE__ */ new Set([
        "capacityClassId",
        "credentialProfileId",
        "fixtureProfileId",
        "permissionProfileId",
        "profileId",
        "runnerProfileId",
        "serviceProfileIds",
        "shardingPolicy"
      ]);
      if (!hasExactKeys(profile, keys) || profile.profileId !== binding.profileId) {
        return false;
      }
      return [
        "runnerProfileId",
        "permissionProfileId",
        "credentialProfileId",
        "fixtureProfileId",
        "capacityClassId"
      ].every((key) => profile[key] === binding[key]) && canonicalStrings(profile.serviceProfileIds, {
        allowEmpty: true,
        maximum: MAX_SERVICE_PROFILE_IDS,
        predicate: (item) => IDENTIFIER.test(item)
      }) && JSON.stringify(profile.serviceProfileIds) === JSON.stringify(binding.serviceProfileIds);
    }
    function validateShardingPolicy(policy) {
      const keys = /* @__PURE__ */ new Set([
        "cpuWeight",
        "maxItemsPerShard",
        "maxParallel",
        "maxShards",
        "operatorWeight",
        "setupSecondsPerShard",
        "wallWeight"
      ]);
      if (!hasExactKeys(policy, keys)) {
        return false;
      }
      const integers = [policy.maxItemsPerShard, policy.maxParallel, policy.maxShards];
      const weights = [policy.cpuWeight, policy.operatorWeight, policy.wallWeight];
      return integers.every((value) => Number.isSafeInteger(value) && value >= 1) && policy.maxItemsPerShard <= MAX_TESTS_PER_SHARD && policy.maxParallel <= MAX_PARALLEL && policy.maxShards <= MAX_SHARDS && policy.maxParallel <= policy.maxShards && weights.every(
        (value) => typeof value === "number" && Number.isFinite(value) && value >= 0 && value <= 1e3
      ) && typeof policy.setupSecondsPerShard === "number" && Number.isFinite(policy.setupSecondsPerShard) && policy.setupSecondsPerShard >= 0 && policy.setupSecondsPerShard <= 3600 && policy.cpuWeight + policy.operatorWeight + policy.wallWeight > 0;
    }
    function deriveProviderSignal(profileId, shardId) {
      const hash = digest(
        stableStringify({
          schemaVersion: "provider-signal/v1",
          executionProfileId: profileId,
          shardId
        })
      );
      return {
        signalId: `provider_signal_${hash.slice(0, 32)}`,
        jobName: `ci/${profileId}/${hash.slice(0, 20)}`,
        kind: "derived-shard",
        executionProfileId: profileId,
        shardId,
        workflowPath: null,
        jobId: null
      };
    }
    function declaredProviderSignal(workflow) {
      const projection = {
        schemaVersion: "declared-native-provider-signal/v1",
        workflowPath: workflow.workflowPath,
        jobId: workflow.gateJobId,
        jobName: workflow.gateSignalName
      };
      const hash = digest(stableStringify(projection));
      return {
        signalId: `provider_signal_${hash.slice(0, 32)}`,
        jobName: workflow.gateSignalName,
        kind: "declared-native",
        executionProfileId: null,
        shardId: null,
        workflowPath: workflow.workflowPath,
        jobId: workflow.gateJobId
      };
    }
    function selectedOutputs(execution, registry) {
      const keys = /* @__PURE__ */ new Set([
        "catalogHash",
        "deterministicPlanId",
        "executionKind",
        "gateProviderSignal",
        "mode",
        "omittedObligationIds",
        "profiles",
        "selectedObligationIds",
        "selectedWitnessIds",
        "targetRegistryHash",
        "testManifestId",
        "verifiedPlanId",
        "workflowPath"
      ]);
      if (!hasExactKeys(execution, keys) || execution.mode !== "selected") {
        return { reason: "selected_execution_shape_invalid" };
      }
      if (execution.executionKind !== registry.workflow.executionKind || execution.workflowPath !== registry.workflow.workflowPath || stableStringify(execution.gateProviderSignal) !== stableStringify(declaredProviderSignal(registry.workflow)) || !/^[0-9a-f]{64}$/.test(execution.catalogHash) || execution.targetRegistryHash !== registry.hash || typeof execution.deterministicPlanId !== "string" || execution.deterministicPlanId.length === 0 || typeof execution.verifiedPlanId !== "string" || execution.verifiedPlanId.length === 0 || !canonicalStrings(execution.selectedObligationIds, {
        predicate: (item) => IDENTIFIER.test(item)
      }) || !canonicalStrings(execution.omittedObligationIds, {
        allowEmpty: true,
        predicate: (item) => IDENTIFIER.test(item)
      }) || !canonicalStrings(execution.selectedWitnessIds, {
        predicate: (item) => IDENTIFIER.test(item)
      }) || execution.selectedObligationIds.some((id) => execution.omittedObligationIds.includes(id)) || !Array.isArray(execution.profiles) || execution.profiles.length === 0 || execution.profiles.length > MAX_PROFILES || !canonicalStrings(
        execution.profiles.map((profile) => profile?.profile?.profileId),
        {
          maximum: MAX_PROFILES,
          predicate: (item) => IDENTIFIER.test(item)
        }
      )) {
        return { reason: "selected_execution_identity_invalid" };
      }
      if (execution.executionKind === "native-job-set") {
        return nativeOutputs(execution, registry);
      }
      if (execution.executionKind !== "witness-shards" || !/^test_manifest_[0-9a-f]{32}$/.test(execution.testManifestId)) {
        return { reason: "selected_execution_identity_invalid" };
      }
      return shardOutputs(execution, registry);
    }
    function nativeOutputs(execution, registry) {
      if (execution.testManifestId !== null) {
        return { reason: "native_execution_manifest_invalid" };
      }
      const keys = /* @__PURE__ */ new Set(["executionKind", "jobId", "profile", "witnessIds"]);
      const selectedJobs = [];
      const coveredWitnessIds = /* @__PURE__ */ new Set();
      for (const signedProfile of execution.profiles) {
        const profileId = signedProfile?.profile?.profileId;
        const binding = registry.profiles.get(profileId);
        if (!hasExactKeys(signedProfile, keys) || signedProfile.executionKind !== "native-job-set" || !binding || binding.workflowPath !== registry.workflow.workflowPath || binding.executionKind !== "native-job-set" || signedProfile.jobId !== binding.jobId || !validateProfileBinding(signedProfile.profile, binding) || !validateShardingPolicy(signedProfile.profile.shardingPolicy) || !canonicalStrings(signedProfile.witnessIds, {
          predicate: (item) => IDENTIFIER.test(item)
        }) || signedProfile.witnessIds.some((witnessId) => coveredWitnessIds.has(witnessId))) {
          return { reason: "native_profile_execution_invalid" };
        }
        signedProfile.witnessIds.forEach((witnessId) => coveredWitnessIds.add(witnessId));
        selectedJobs.push(binding.jobId);
      }
      const rootJobIds = [...selectedJobs].sort(compareUtf16CodeUnits);
      const executableJobIds = dependencyClosure(
        rootJobIds,
        registry.workflow.executionJobs
      );
      if (registry.workflow.requiredJobIds.some(
        (jobId) => !executableJobIds.includes(jobId)
      )) {
        return { reason: "required_target_job_missing" };
      }
      if (rootJobIds.length !== new Set(rootJobIds).size || execution.selectedWitnessIds.length !== coveredWitnessIds.size || execution.selectedWitnessIds.some((id) => !coveredWitnessIds.has(id))) {
        return { reason: "selected_witness_coverage_invalid" };
      }
      return {
        matrices: {},
        maxParallelByJob: {},
        selectedJobs: executableJobIds
      };
    }
    function dependencyClosure(rootJobIds, executionJobs) {
      const known = new Set(executionJobs.map((job) => job.jobId));
      const dependencies = new Map(
        executionJobs.map((job) => [
          job.jobId,
          job.needs.filter((dependency) => known.has(dependency))
        ])
      );
      const selected = new Set(rootJobIds);
      const pending = [...rootJobIds];
      while (pending.length > 0) {
        const jobId = pending.pop();
        for (const dependency of dependencies.get(jobId)) {
          if (!selected.has(dependency)) {
            selected.add(dependency);
            pending.push(dependency);
          }
        }
      }
      return [...selected].sort(compareUtf16CodeUnits);
    }
    function shardOutputs(execution, registry) {
      const matricesByJob = /* @__PURE__ */ new Map();
      const maxParallelByJob = /* @__PURE__ */ new Map();
      const seenShardIds = /* @__PURE__ */ new Set();
      const seenTestIds = /* @__PURE__ */ new Set();
      const coveredWitnessIds = /* @__PURE__ */ new Set();
      const profileKeys = /* @__PURE__ */ new Set([
        "capacityMode",
        "capacityReason",
        "executionKind",
        "maxParallel",
        "profile",
        "shards"
      ]);
      const shardKeys = /* @__PURE__ */ new Set([
        "executionProfileId",
        "manifestId",
        "providerSignal",
        "shardId",
        "testIds",
        "witnessIds"
      ]);
      for (const signedProfile of execution.profiles) {
        const profileId = signedProfile?.profile?.profileId;
        const binding = registry.profiles.get(profileId);
        if (!hasExactKeys(signedProfile, profileKeys) || signedProfile.executionKind !== "witness-shards" || !binding || binding.workflowPath !== registry.workflow.workflowPath || binding.executionKind !== "witness-shards" || !validateProfileBinding(signedProfile.profile, binding) || !validateShardingPolicy(signedProfile.profile.shardingPolicy) || !Array.isArray(signedProfile.shards) || !canonicalStrings(
          signedProfile.shards.map((shard) => shard?.shardId),
          {
            maximum: signedProfile.profile.shardingPolicy.maxShards,
            predicate: (item) => /^ci_shard_[0-9a-f]{32}$/.test(item)
          }
        ) || !Number.isSafeInteger(signedProfile.maxParallel) || signedProfile.maxParallel < 1 || signedProfile.maxParallel > Math.min(signedProfile.shards.length, MAX_PARALLEL) || signedProfile.maxParallel > signedProfile.profile.shardingPolicy.maxParallel || !["optimized", "conservative"].includes(signedProfile.capacityMode) || signedProfile.capacityMode === "optimized" && signedProfile.capacityReason !== null || signedProfile.capacityMode === "conservative" && (typeof signedProfile.capacityReason !== "string" || signedProfile.capacityReason.length === 0 || signedProfile.capacityReason.length > 200)) {
          return { reason: "profile_execution_invalid" };
        }
        const include = [];
        for (const shard of signedProfile.shards) {
          if (!hasExactKeys(shard, shardKeys) || !/^ci_shard_[0-9a-f]{32}$/.test(shard.shardId) || seenShardIds.has(shard.shardId) || shard.manifestId !== execution.testManifestId || shard.executionProfileId !== profileId || !canonicalStrings(shard.witnessIds, {
            predicate: (item) => IDENTIFIER.test(item)
          }) || !canonicalStrings(shard.testIds, {
            maximum: signedProfile.profile.shardingPolicy.maxItemsPerShard,
            predicate: (item) => item.length <= 256 && /^[\x20-\x7E]+$/.test(item)
          })) {
            return { reason: "execution_shard_invalid" };
          }
          const expectedSignal = deriveProviderSignal(profileId, shard.shardId);
          if (stableStringify(shard.providerSignal) !== stableStringify(expectedSignal)) {
            return { reason: "provider_signal_invalid" };
          }
          if (shard.testIds.some((testId) => seenTestIds.has(testId))) {
            return { reason: "execution_test_identity_duplicate" };
          }
          seenShardIds.add(shard.shardId);
          shard.testIds.forEach((testId) => seenTestIds.add(testId));
          if (seenTestIds.size > MAX_TESTS) {
            return { reason: "execution_test_count_exceeded" };
          }
          shard.witnessIds.forEach((witnessId) => coveredWitnessIds.add(witnessId));
          include.push({
            provider_job_name: expectedSignal.jobName,
            shard_json: JSON.stringify({
              schemaVersion: "dynamic-ci-shard/v1",
              shardId: shard.shardId,
              manifestId: shard.manifestId,
              executionProfileId: shard.executionProfileId,
              witnessIds: shard.witnessIds,
              testIds: shard.testIds,
              providerSignal: expectedSignal
            })
          });
        }
        matricesByJob.set(binding.jobId, { include });
        maxParallelByJob.set(binding.jobId, signedProfile.maxParallel);
      }
      if (execution.selectedWitnessIds.length !== coveredWitnessIds.size || execution.selectedWitnessIds.some((id) => !coveredWitnessIds.has(id))) {
        return { reason: "selected_witness_coverage_invalid" };
      }
      const selectedJobs = [...matricesByJob].map(([jobId]) => jobId).sort(compareUtf16CodeUnits);
      const selectedClosure = dependencyClosure(
        selectedJobs,
        registry.workflow.executionJobs
      );
      if (selectedClosure.length !== selectedJobs.length || selectedClosure.some((jobId, index) => jobId !== selectedJobs[index])) {
        return { reason: "selected_job_dependency_missing" };
      }
      if (registry.workflow.requiredJobIds.some(
        (jobId) => !selectedJobs.includes(jobId)
      )) {
        return { reason: "required_target_job_missing" };
      }
      return {
        matrices: Object.fromEntries(selectedJobs.map((jobId) => [jobId, matricesByJob.get(jobId)])),
        maxParallelByJob: Object.fromEntries(
          selectedJobs.map((jobId) => [jobId, maxParallelByJob.get(jobId)])
        ),
        selectedJobs
      };
    }
    function validatePayload(payload, registry) {
      const bindingError = validateBinding(payload);
      if (bindingError) {
        return { reason: bindingError };
      }
      const authError = validateAuthenticatedRun(payload.authenticatedRun);
      if (authError) {
        return { reason: authError };
      }
      const authenticatedRun = payload.authenticatedRun;
      let expectedRequesterRef = registry.workflow.planRequestWorkflowRef;
      let expectedRequesterSha = expectedRequesterRef.slice(
        expectedRequesterRef.lastIndexOf("@") + 1
      );
      if (expectedRequesterRef === LOCAL_REQUESTER_REF) {
        const callerPrefix = `${authenticatedRun.repository}/${registry.workflow.workflowPath}@`;
        const callerRef = authenticatedRun.workflowRef;
        if (typeof callerRef !== "string" || !callerRef.startsWith(callerPrefix)) {
          return { reason: "auth_job_workflow_ref_mismatch" };
        }
        const revisionRef = callerRef.slice(callerPrefix.length);
        if (!revisionRef.startsWith("refs/") || revisionRef.length <= 5) {
          return { reason: "auth_job_workflow_ref_mismatch" };
        }
        expectedRequesterRef = `${authenticatedRun.repository}/${LOCAL_REQUESTER_PATH}@${revisionRef}`;
        expectedRequesterSha = authenticatedRun.workflowSha;
        if (typeof expectedRequesterSha !== "string" || !/^[0-9a-f]{40}$/.test(expectedRequesterSha)) {
          return { reason: "auth_job_workflow_sha_mismatch" };
        }
      }
      if (authenticatedRun.jobWorkflowRef !== expectedRequesterRef) {
        return { reason: "auth_job_workflow_ref_mismatch" };
      }
      if (authenticatedRun.jobWorkflowSha !== expectedRequesterSha) {
        return { reason: "auth_job_workflow_sha_mismatch" };
      }
      if (payload.verifiedPlanId === null && payload.productionAdmissionReceiptId === null && payload.verifierVersion === null && typeof payload.fallbackReason === "string" && hasExactKeys(payload.execution, /* @__PURE__ */ new Set(["mode", "reason"])) && payload.execution.mode === "full-ci" && payload.execution.reason === payload.fallbackReason) {
        return { fallback: true, reason: payload.fallbackReason };
      }
      if (typeof payload.planId !== "string" || payload.planId !== payload.verifiedPlanId || !/^production_admission_[0-9a-f]{32}$/.test(payload.productionAdmissionReceiptId) || typeof payload.verifierVersion !== "string" || payload.verifierVersion.length === 0 || payload.fallbackReason !== null || payload.execution?.verifiedPlanId !== payload.verifiedPlanId) {
        return { reason: "signed_plan_state_inconsistent" };
      }
      return selectedOutputs(payload.execution, registry);
    }
    module2.exports = Object.freeze({
      dependencyClosure,
      loadRegistry,
      loadStaticJobIds,
      loadWorkflowPath,
      validatePayload
    });
  }
});

// validate_gate.cjs
var require_validate_gate = __commonJS({
  "validate_gate.cjs"(exports2, module2) {
    "use strict";
    var {
      JOB_ID,
      MAX_PROFILES,
      canonicalStrings,
      compareUtf16CodeUnits
    } = require_validation_core();
    var {
      dependencyClosure,
      loadRegistry,
      loadWorkflowPath
    } = require_validation_execution();
    var JOB_RESULTS = /* @__PURE__ */ new Set(["cancelled", "failure", "skipped", "success"]);
    var MAX_GATE_INPUT_BYTES = 131072;
    function parseJsonEnvironment(name, fallback) {
      const text = process.env[name] || fallback;
      if (Buffer.byteLength(text, "utf8") > MAX_GATE_INPUT_BYTES) {
        throw new Error("gate_input_too_large");
      }
      try {
        return JSON.parse(text);
      } catch {
        throw new Error("gate_input_invalid");
      }
    }
    function loadStaticJobResults() {
      const value = parseJsonEnvironment("CI_STATIC_JOB_RESULTS_JSON", "");
      if (value === null || typeof value !== "object" || Array.isArray(value)) {
        throw new Error("static_job_results_invalid");
      }
      const jobIds = Object.keys(value).sort(compareUtf16CodeUnits);
      if (!canonicalStrings(jobIds, {
        maximum: MAX_PROFILES,
        predicate: (item) => JOB_ID.test(item)
      }) || jobIds.some((jobId) => !JOB_RESULTS.has(value[jobId]))) {
        throw new Error("static_job_results_invalid");
      }
      return { jobIds, results: value };
    }
    function loadSelectedJobs() {
      const value = parseJsonEnvironment("CI_SELECTED_JOBS_JSON", "[]");
      if (!canonicalStrings(value, {
        allowEmpty: true,
        maximum: MAX_PROFILES,
        predicate: (item) => JOB_ID.test(item)
      })) {
        throw new Error("selected_jobs_invalid");
      }
      return value;
    }
    function main2() {
      try {
        const { jobIds, results } = loadStaticJobResults();
        const workflowPath = loadWorkflowPath();
        const registry = loadRegistry(new Set(jobIds), workflowPath);
        const selectedJobs = loadSelectedJobs();
        const selected = new Set(selectedJobs);
        const selectedWithinRegistry = selectedJobs.every((jobId) => jobIds.includes(jobId));
        const selectedClosure = selectedWithinRegistry ? dependencyClosure(selectedJobs, registry.workflow.executionJobs) : [];
        const selectedIsClosed = selectedWithinRegistry && selectedClosure.length === selectedJobs.length && selectedClosure.every((jobId, index) => jobId === selectedJobs[index]);
        const fullCiResult = process.env.CI_FULL_CI_RESULT || "";
        const alternatePathInactive = registry.workflow.executionKind === "native-job-set" ? fullCiResult === "" : fullCiResult === "skipped";
        const selectedMode = process.env.CI_PLAN_RESULT === "success" && process.env.CI_PLAN_VALID === "true" && process.env.CI_FALLBACK === "false";
        const selectedAdmitted = selectedMode && selected.size > 0 && registry.workflow.requiredJobIds.every((jobId) => selected.has(jobId)) && selectedIsClosed && alternatePathInactive && jobIds.every(
          (jobId) => results[jobId] === (selected.has(jobId) ? "success" : "skipped")
        );
        const fallbackAdmitted = !selectedMode && selected.size === 0 && (registry.workflow.executionKind === "native-job-set" ? fullCiResult === "" && jobIds.every((jobId) => results[jobId] === "success") : fullCiResult === "success" && jobIds.every((jobId) => results[jobId] === "skipped"));
        process.exitCode = selectedAdmitted || fallbackAdmitted ? 0 : 1;
      } catch {
        process.exitCode = 1;
      }
    }
    module2.exports = Object.freeze({ main: main2 });
  }
});

// validate_plan.cjs
var require_validate_plan = __commonJS({
  "validate_plan.cjs"(exports2, module2) {
    "use strict";
    var {
      MAX_ENVELOPE_BYTES,
      failClosed,
      finish,
      readJson,
      readPlanTrustRoot
    } = require_validation_core();
    var { verifyEnvelope } = require_validation_envelope();
    var {
      loadRegistry,
      loadStaticJobIds,
      loadWorkflowPath,
      validatePayload
    } = require_validation_execution();
    function main2() {
      try {
        const response = readJson(process.env.PLAN_PATH, MAX_ENVELOPE_BYTES).value;
        if (response?.schemaVersion === "dynamic-ci-plan/v0" && response.fallback === true) {
          finish(false, true, response.reason ?? "unsigned_fallback_response");
          return;
        }
        const trustRoot = readPlanTrustRoot();
        const envelopeError = verifyEnvelope(response, trustRoot.publicKey, trustRoot.keyId);
        if (envelopeError) {
          failClosed(envelopeError);
          return;
        }
        const registry = loadRegistry(loadStaticJobIds(), loadWorkflowPath());
        const result = validatePayload(response.payload, registry);
        if (result.reason && !result.fallback) {
          failClosed(result.reason);
          return;
        }
        if (result.fallback) {
          finish(true, true, result.reason);
          return;
        }
        finish(
          true,
          false,
          "signed_plan_valid",
          result.matrices,
          result.maxParallelByJob,
          result.selectedJobs
        );
      } catch (error) {
        failClosed(error instanceof Error ? error.message : "plan_validation_failed");
      }
    }
    module2.exports = Object.freeze({ main: main2 });
  }
});

// main.cjs
var { main: consumePlan } = require_consume_plan();
var { main: validateGate } = require_validate_gate();
var { main: validatePlan } = require_validate_plan();
var COMMANDS = Object.freeze({
  "consume-plan": consumePlan,
  "validate-gate": validateGate,
  "validate-plan": validatePlan
});
function main() {
  const arguments_ = process.argv.slice(2);
  if (arguments_.length !== 1) {
    process.exitCode = 1;
    return;
  }
  const command = Object.hasOwn(COMMANDS, arguments_[0]) ? COMMANDS[arguments_[0]] : void 0;
  if (command === void 0) {
    process.exitCode = 1;
    return;
  }
  command();
}
main();
