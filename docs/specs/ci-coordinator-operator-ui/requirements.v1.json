{
  "schemaVersion": 1,
  "sourceId": "ci-coordinator.operator-ui.requirements",
  "specPackagePath": "docs/specs/ci-coordinator-operator-ui",
  "requirementsPath": "docs/specs/ci-coordinator-operator-ui/requirements.v1.json",
  "overviewPath": "docs/specs/ci-coordinator-operator-ui/overview.md",
  "requirements": [
    {
      "requirementId": "REQ-CI-UI-001",
      "ownerId": "ci-coordinator.operator-ui",
      "invariant": "The browser admits installation id, repository id, and item limit against the served request bounds before network access, applies a finite transport timeout, and renders repository workbench evidence only after the same-origin response has matched the requested scope and passed bounded runtime schema validation; timed-out requests become network failure, while malformed, cross-scope, or unsupported data is invalid response and never evidence.",
      "claimLevel": "blocking",
      "riskClass": "critical",
      "proofBindingRefs": ["proofkit/requirement-bindings.json"],
      "nonClaimRefs": ["NC-CI-UI-001"],
      "nonClaims": ["This requirement does not prove backend evidence correctness, provider freshness, or production browser authentication."],
      "lifecycle": {"state": "active", "replacementRequirementIds": [], "evidenceRefs": []},
      "deferral": null,
      "updatePolicy": {"reviewOwnerId": "ci-coordinator.operator-ui", "requiresImpactDeclaration": true, "requiresProofBindingReview": true}
    },
    {
      "requirementId": "REQ-CI-UI-002",
      "ownerId": "ci-coordinator.operator-ui",
      "invariant": "No break-glass or machine bearer, Keycloak or provider token, local credential, private key, or database secret is accepted by browser UI, persisted in browser storage, emitted by a browser-visible endpoint, logged by frontend code, mounted into the frontend development container, or included in source maps and production assets. The development proxy delegates only the closed control-plane method, canonical path, and bounded query allowlist for identity, repository inventory, workbench, workflow discovery, governance, repository attestation, config activation, scoped economics reads, explicit source discovery and registration to either the exact Compose backend or an explicit loopback HTTP origin; report ingestion is excluded. It injects no authorization header, and no other target, operation, query, or out-of-contract scope receives that capability.",
      "claimLevel": "blocking",
      "riskClass": "critical",
      "proofBindingRefs": ["proofkit/requirement-bindings.json"],
      "nonClaimRefs": ["NC-CI-UI-002"],
      "nonClaims": ["This requirement does not configure or deploy Keycloak, prove a live provider callback, or authorize a production browser session."],
      "lifecycle": {"state": "active", "replacementRequirementIds": [], "evidenceRefs": []},
      "deferral": null,
      "updatePolicy": {"reviewOwnerId": "ci-coordinator.operator-ui", "requiresImpactDeclaration": true, "requiresProofBindingReview": true}
    },
    {
      "requirementId": "REQ-CI-UI-003",
      "ownerId": "ci-coordinator.operator-ui",
      "invariant": "Loading, empty, unauthenticated, forbidden, unavailable, invalid-response, network-failure, truncated, and non-valid replay states remain distinguishable and cannot be rendered with the visual or semantic success treatment reserved for validated evidence; the UI displays the backend observation time but does not infer freshness without a specified freshness policy. Pending reads expose a labelled indeterminate progress indicator with no invented completion value; initial and replacement loading may show inert skeletons, while existing same-scope governance refresh retains explicitly labelled previous observations and disables baseline approval. Reduced-motion preference removes loading animation without hiding the pending state.",
      "claimLevel": "blocking",
      "riskClass": "high",
      "proofBindingRefs": ["proofkit/requirement-bindings.json"],
      "nonClaimRefs": ["NC-CI-UI-003"],
      "nonClaims": ["This requirement does not define backend readiness or reconciliation success semantics."],
      "lifecycle": {"state": "active", "replacementRequirementIds": [], "evidenceRefs": []},
      "deferral": null,
      "updatePolicy": {"reviewOwnerId": "ci-coordinator.operator-ui", "requiresImpactDeclaration": true, "requiresProofBindingReview": true}
    },
    {
      "requirementId": "REQ-CI-UI-004",
      "ownerId": "ci-coordinator.operator-ui",
      "invariant": "The checked-in frontend transport projection is deterministically generated from the served FastAPI OpenAPI contract, generated artifacts are reproducible, and contract drift fails before build or merge rather than being repaired by permissive client casts.",
      "claimLevel": "blocking",
      "riskClass": "high",
      "proofBindingRefs": ["proofkit/requirement-bindings.json"],
      "nonClaimRefs": ["NC-CI-UI-004"],
      "nonClaims": ["This requirement does not make OpenAPI a domain-semantics authority or replace runtime response validation."],
      "lifecycle": {"state": "active", "replacementRequirementIds": [], "evidenceRefs": []},
      "deferral": null,
      "updatePolicy": {"reviewOwnerId": "ci-coordinator.operator-ui", "requiresImpactDeclaration": true, "requiresProofBindingReview": true}
    },
    {
      "requirementId": "REQ-CI-UI-005",
      "ownerId": "ci-coordinator.operator-ui",
      "invariant": "Every interactive control has a semantic accessible name and keyboard operation, status is not conveyed by color alone, focus remains visible, and the workbench preserves readable non-overlapping content in the blocking Playwright Chromium profiles for Desktop Chrome, Pixel 7 touch/mobile emulation, and a 320 by 800 narrow reflow viewport. Numeric run and job identifiers and ledger revisions retain their ungrouped decimal representation; observed instants use the shared UTC display. Full identifiers remain inspectable and explicit clipboard copy uses the original value, distinguishes denial from success, and cannot apply a late result to another identity. Policy pretty-printing is presentation-only and never changes admitted command source. The visual system uses a navy sidebar, light canvas, locally bundled Lato, consistent framed summaries and forms, compact semantic tables, restrained textual status chips and visible focus on both dark and light surfaces; every runtime-mode label requires an admitted source operand and is absent when the UI contract supplies none. The Organization selector remains a labelled native select with unchanged keyboard, focus and disabled semantics. Its sole decorative chevron is non-focusable, hidden from assistive technology, pointer-transparent, vertically centered and explicitly inset from the inline-end border with separate text space. Forced-color mode restores the native arrow instead of rendering both indicators; long labels and contextual controls remain within supported viewport bounds.",
      "claimLevel": "blocking",
      "riskClass": "high",
      "proofBindingRefs": ["proofkit/requirement-bindings.json"],
      "nonClaimRefs": ["NC-CI-UI-005"],
      "nonClaims": ["This requirement does not claim Firefox, WebKit, other device/browser combinations, virtual-keyboard behavior, orientation changes, or conformance for untested assistive-technology combinations."],
      "lifecycle": {"state": "active", "replacementRequirementIds": [], "evidenceRefs": []},
      "deferral": null,
      "updatePolicy": {"reviewOwnerId": "ci-coordinator.operator-ui", "requiresImpactDeclaration": true, "requiresProofBindingReview": true}
    },
    {
      "requirementId": "REQ-CI-UI-006",
      "ownerId": "ci-coordinator.operator-ui",
      "invariant": "The frontend uses an exact Node and pnpm package closure, strict TypeScript and static analysis, component-state falsifiers, production build verification, browser rendering at admitted viewports, and an accessibility scan; development fixtures and proxy-only code are absent from production assets. An isolated image builder may receive deployment-owned routing only through Docker predefined proxy arguments; Node environment-proxy routing is enabled only in the disposable operator-UI build stage, and the final runtime image inherits neither that switch nor a proxy variable. The supported container embeds that admitted bundle and serves the no-store workbench shell plus immutable hashed assets from the backend origin with a restrictive browser security policy. Bundled font assets retain their exact provenance and license; browser witnesses verify same-origin font loading under the enforced shell policy and license retention in the production stylesheet. When the UI bundle is mounted, GET and HEAD on the exact root path return a no-store 307 redirect to fixed relative /workbench with common UI security headers and no credential or authority change. In Keycloak-enabled mode, GET/HEAD on both workbench paths checks a fresh opaque human session and the read role before returning HTML; missing or expired sessions redirect only to the fixed local login start, ambiguous credentials or missing roles reject, and session dependency unavailability or overload returns 503 without a redirect loop. Static assets remain public; non-Keycloak and disabled behavior and API credential planes are unchanged. All page outcomes are no-store. Caller query, Host and forwarded headers cannot change its target; UI-disabled mode exposes no root redirect and unknown paths are not an SPA fallback. Blocking browser proof rejects focused declarations through the pinned runner and error-level static policy, with actual CLI positive and isolated negative controls. The operator job preserves its fail-fast production build and verifies that exact complete bundle before browser execution without rebuilding it; standalone browser invocation remains build-complete.",
      "claimLevel": "blocking",
      "riskClass": "high",
      "proofBindingRefs": ["proofkit/requirement-bindings.json"],
      "nonClaimRefs": ["NC-CI-UI-006"],
      "nonClaims": ["This requirement does not admit or prove the secrecy, syntax, availability, or operator provenance of externally supplied build-proxy values; builder availability, deployment, runtime SLOs, and exhaustive browser compatibility also remain unproved."],
      "lifecycle": {"state": "active", "replacementRequirementIds": [], "evidenceRefs": []},
      "deferral": null,
      "updatePolicy": {"reviewOwnerId": "ci-coordinator.operator-ui", "requiresImpactDeclaration": true, "requiresProofBindingReview": true}
    },
    {
      "requirementId": "REQ-CI-UI-007",
      "ownerId": "ci-coordinator.operator-ui",
      "invariant": "The primary workbench trajectory discovers operator-authorized GitHub App organization installations and their searchable paginated repositories without requiring numeric identifiers; preserves installation-and-repository identity across every request and admitted response; distinguishes GitHub visibility, structural repository state, and exact evidence-access authorization; and renders valid empty, partial, suspended, unsupported, unavailable, forbidden, rate-limited, invalid-response, and network-failure states without inventing completeness, configuration, or enforcement. Browser response admission rejects aggregate-bound overflow, inactive or cross-installation pages, repository owner ids outside the installation account, non-Unicode-scalar provider text, and contradictory pagination. Manual numeric scope entry is available only through an explicitly labelled degraded-access disclosure and remains governed by the existing exact workbench contract. App-wide mode discovers current installations with bounded organization pagination and no per-installation configuration; delayed responses are bound to the requested page and selection lifetime, and refreshing or paging cannot leave an absent installation actionable. In each ready catalog, pagination is visible if and only if that catalog's current page is greater than one or its admitted hasNextPage is true; Previous is disabled only on page one and Next only without continuation. Organization pagination belongs to the selector's toolbar group, outside its label; repository pagination belongs to its table. Empty or filtered rows and incomplete status cannot remove an available back or forward path. Hiding non-actionable pagination neither fetches additional pages automatically nor changes selection, loading, failure, explicit retry, request bounds or incomplete-catalog disclosure.",
      "claimLevel": "blocking",
      "riskClass": "critical",
      "proofBindingRefs": ["proofkit/requirement-bindings.json"],
      "nonClaimRefs": ["NC-CI-UI-007"],
      "nonClaims": ["This requirement does not claim production browser authentication, workflow scanning, proposal generation, repository mutation, or provider administration."],
      "lifecycle": {"state": "active", "replacementRequirementIds": [], "evidenceRefs": []},
      "deferral": null,
      "updatePolicy": {"reviewOwnerId": "ci-coordinator.operator-ui", "requiresImpactDeclaration": true, "requiresProofBindingReview": true}
    },
    {
      "requirementId": "REQ-CI-UI-008",
      "ownerId": "ci-coordinator.operator-ui",
      "invariant": "After an operator selects an evidence-authorized repository, the browser may request workflow discovery at the resolved default head or one locally admitted exact lowercase 40-hex historical revision, but renders the result only after the same-origin response passes bounded runtime validation and matches the requested installation id, repository id, and explicit revision when supplied. The workbench exposes exact snapshot identity, completeness, workflows, jobs, call edges, proven assertions, every explicit unknown, and either reviewable conservative policy bytes or typed blocking reasons through keyboard-accessible progressive disclosure. Before treating a proposal as reviewable, the browser requires a complete locally closed report from the exact supported generator; independently re-admits the proven default-branch event coverage, sole-job or complete acyclic unique-sink topology, and global provider-signal collision guard; strictly binds the decoded observe-only policy source to the selected repository, events, default branch, workflow, and signal; and recomputes the canonical SHA-256 proposal manifest identity with built-in Web Crypto. An explicit historical request cannot return a reviewable proposal. Historical, incomplete, ambiguous, malformed, unavailable, stale-response, and network-failure states remain distinct and cannot receive reviewable treatment. Repository-provided text is rendered only as inert data. Discovery itself grants no review or activation authority and exposes no dispatch, provider-write, secret, or omission-enablement capability; separately authorized repository attestation and config activation are governed by REQ-CI-UI-009 and REQ-CI-UI-010.",
      "claimLevel": "blocking",
      "riskClass": "critical",
      "proofBindingRefs": ["proofkit/requirement-bindings.json"],
      "nonClaimRefs": ["NC-CI-UI-008"],
      "nonClaims": ["This requirement does not prove backend evidence correctness, workflow runtime behavior, production browser authentication, owner approval, provider administration, configuration activation, omission safety, deployment approval, or production readiness."],
      "lifecycle": {"state": "active", "replacementRequirementIds": [], "evidenceRefs": []},
      "deferral": null,
      "updatePolicy": {"reviewOwnerId": "ci-coordinator.operator-ui", "requiresImpactDeclaration": true, "requiresProofBindingReview": true}
    },
    {
      "requirementId": "REQ-CI-UI-009",
      "ownerId": "ci-coordinator.operator-ui",
      "invariant": "The production browser shell obtains authentication state only from the bounded same-origin control-plane session endpoint, never receives a Keycloak, provider, machine, or deployment credential, and distinguishes loading, challenged, anonymous, authenticated, overloaded, unavailable, invalid-response and network-failure. Login navigates only to the fixed same-origin Keycloak start endpoint; logout is an explicit CSRF-protected mutation. Credentials and CSRF remain absent from browser persistence, URLs, logs and assets. Bounded inert identity metadata is never authority. Expiry, explicit refresh, current cookie-API 401 and logout intent invalidate the previous authority generation, abort obsolete requests and discard its evidence and drafts; late responses cannot restore it. Expiry or a current same-origin v1/v2 cookie-API 401, excluding identity endpoints and explicit Authorization, permits one coalesced session recheck; the same admitted session cannot repeatedly recheck on feature 401 responses. A repeated current challenge still invalidates UI authority and requires an explicit check. Subscription installation precedes descendant passive request effects. Failure never becomes authentication or automatic login, and client-past expiry cannot create a retry loop. Only a previously authenticated page followed by freshly confirmed anonymous admission may attempt automatic ordinary SSO while visible, at most once per two minutes per tab with a durable local loop guard. Initial anonymous state, logout intent, absent storage or a malformed/future guard requires explicit action. An optional ten-minute one-use same-tab hint contains only version, attempt time and canonical local workbench scope/view/evidence-tab coordinates; it is readmitted and consumed after fresh authentication, grants no authority and contains no command or secret. Reading a malformed hint preserves its automatic-login veto. Failed logout remains a visible sign-out failure with remote completion unconfirmed. Logout clears the hint and fences pending admission; no recovery retries a mutation or claims an aborted write failed. Visible-page resumption checks expiry without background polling. Development proxy and frontend image receive no authorization credential or secret. The optional return additionally admits canonical nondefault Economics tabs and Activity sources for the matching view and scope; these inert coordinates alone survive renewal, while credentials, commands, filters, drafts and old responses do not.",
      "claimLevel": "blocking",
      "riskClass": "critical",
      "proofBindingRefs": ["proofkit/requirement-bindings.json"],
      "nonClaimRefs": ["NC-CI-UI-009"],
      "nonClaims": ["This requirement does not prove backend token exchange, provider availability, enterprise SSO state, production secret custody, deployment, or repository authorization."],
      "lifecycle": {"state": "active", "replacementRequirementIds": [], "evidenceRefs": []},
      "deferral": null,
      "updatePolicy": {"reviewOwnerId": "ci-coordinator.operator-ui", "requiresImpactDeclaration": true, "requiresProofBindingReview": true}
    },
    {
      "requirementId": "REQ-CI-UI-010",
      "ownerId": "ci-coordinator.operator-ui",
      "invariant": "For a complete locally re-admitted current-head reviewable proposal, an authenticated Keycloak human may start one explicit repository-authority verification bound to the selected installation and repository, proposal manifest, exact active epoch pointer or absence, and a new bounded operation id. The mutation includes same-origin credentials and the in-memory session CSRF proof, disables duplicate submission, and accepts only the fixed GitHub authorization URL returned by the exact endpoint. After the exact callback returns to the same scope and proposal, an administrator with the activate role may submit a distinct bounded activation operation carrying the exact proposal manifest, target epoch, and expected active revision. The UI presents verification and activation as separate ordered steps, never treats the callback hint as activation proof, and states that activation performs a fresh authority recheck. Idle, starting, redirecting, reviewed, submitting, active, duplicate, stale, baseline-conflict, operation-conflict, epoch-conflict, attestation-invalid, revision-conflict, coverage failure, overloaded, unavailable, forbidden, unauthenticated, invalid-response, and network-failure states remain distinguishable; an older request generation cannot overwrite a newer authority, scope, proposal, review, or activation. Role absence disables the corresponding command without inventing authority. No control exposes provider credentials, provider-write, workflow-dispatch, secret, or omission authority. Every control is keyboard operable with a visible focus state and semantic accessible name, and state is not conveyed by color alone at admitted mobile and desktop viewports.",
      "claimLevel": "blocking",
      "riskClass": "critical",
      "proofBindingRefs": ["proofkit/requirement-bindings.json"],
      "nonClaimRefs": ["NC-CI-UI-010"],
      "nonClaims": ["This requirement does not prove backend authorization, receipt or activation atomicity, current provider permission, or server-side activation admission; it does not mutate GitHub, authorize CI omission, provide review quorum or rejection comments, approve deployment, or establish production readiness."],
      "lifecycle": {"state": "active", "replacementRequirementIds": [], "evidenceRefs": []},
      "deferral": null,
      "updatePolicy": {"reviewOwnerId": "ci-coordinator.operator-ui", "requiresImpactDeclaration": true, "requiresProofBindingReview": true}
    },
    {
      "requirementId": "REQ-CI-UI-011",
      "ownerId": "ci-coordinator.operator-ui",
      "invariant": "For one evidence-authorized selected repository, the browser may receive the read-only effective-governance observation either from its bounded endpoint or as the observation subprojection of composed governance evidence, but renders it only after bounded same-origin runtime admission proves the exact requested installation and repository ids, canonical owner/name/full-name and default-branch relation, supported provider API version, best-effort consistency, lowercase SHA-256 state digest, canonically ordered unique bounded rule set, and complete canonical JSON for every rule. The observation subview distinguishes loading, valid empty, unauthenticated, forbidden, not-found, rate-limited, unavailable, malformed-provider, binding-mismatch, observation-limit, invalid-response, and network-failure states; labels every observation Observation only so it is not confused with an approved expected baseline; renders provider text only as inert data through keyboard-accessible progressive disclosure; and aborts and discards stale authority or scope generations before rendering them. Observation evidence itself grants no baseline approval, compliance, drift, activation, provider-write, dispatch, release, secret, or omission authority.",
      "claimLevel": "blocking",
      "riskClass": "critical",
      "proofBindingRefs": ["proofkit/requirement-bindings.json"],
      "nonClaimRefs": ["NC-CI-UI-011"],
      "nonClaims": ["This requirement does not prove backend evidence correctness, provider availability, snapshot isolation, stable rule membership across pagination pages, a complete rule set at any single instant, baseline approval, compliance, drift, provider enforcement, release readiness, CI omission safety, deployment, or production readiness."],
      "lifecycle": {"state": "active", "replacementRequirementIds": [], "evidenceRefs": []},
      "deferral": null,
      "updatePolicy": {"reviewOwnerId": "ci-coordinator.operator-ui", "requiresImpactDeclaration": true, "requiresProofBindingReview": true}
    },
    {
      "requirementId": "REQ-CI-UI-012",
      "ownerId": "ci-coordinator.operator-ui",
      "invariant": "For one evidence-authorized selected repository, the browser may read an explicit absent or active governance baseline and may issue one explicit approval or replacement command bound to the exact installation id, repository id, freshly rendered observation digest, complete active-baseline pointer or absence, bounded canonical reason, and new operation id. The mutation carries same-origin credentials and in-memory session CSRF proof, disables duplicate submission, and admits a response only after exact scope, request-operation, pointer, and complete retained-state validation recomputes the advertised digest. Accepted, duplicate, unchanged, stale, baseline-conflict, operation-conflict, unauthenticated, forbidden, unavailable, invalid-response, and network-failure states remain distinguishable. Authority, scope, observation, active-baseline, comparison-read, and request generations disable mutation, abort in-flight work, and discard incomplete older outcomes before they can render or overwrite newer evidence. An already completed exact-command receipt may remain visible as historical operation evidence only within the same non-secret authority revision, exact repository scope, and observation digest; it cannot replace current query evidence or gain retry authority under a newer binding. The CSRF token remains transport-only input and is never retained in the receipt identity. Retained provider text is inert and progressively disclosed; the interface labels the artifact only as an approved expected baseline and exposes no compliance, drift, provider-write, activation, dispatch, release, secret, or omission claim or control.",
      "claimLevel": "blocking",
      "riskClass": "critical",
      "proofBindingRefs": ["proofkit/requirement-bindings.json"],
      "nonClaimRefs": ["NC-CI-UI-012"],
      "nonClaims": ["This requirement does not prove backend authorization, provider freshness, snapshot isolation, persistence atomicity, current baseline conformance, policy-classified drift, provider enforcement, release readiness, CI omission safety, deployment, or production readiness."],
      "lifecycle": {"state": "active", "replacementRequirementIds": [], "evidenceRefs": []},
      "deferral": null,
      "updatePolicy": {"reviewOwnerId": "ci-coordinator.operator-ui", "requiresImpactDeclaration": true, "requiresProofBindingReview": true}
    },
    {
      "requirementId": "REQ-CI-UI-013",
      "ownerId": "ci-coordinator.operator-ui",
      "invariant": "For one evidence-authorized selected repository, the workbench may request one composed read-only governance evidence response containing the fresh best-effort observation, an explicit absent or complete active baseline, and either unbaselined or an exact matches-or-differs comparison. Before rendering, a same-origin runtime admission bounded by the proved 32 MiB composed-response budget establishes the exact requested scope, independently recomputes both complete governance-state digests, binds the active pointer to the retained baseline state, and recomputes the complete relation, closed changed-coordinate ledger, and exact canonical-rule set counts. Loading, unbaselined, matches, differs, stale, unauthenticated, forbidden, not-found, rate-limited, unavailable, malformed-provider, binding-mismatch, observation-limit, invalid-response, and network-failure states remain distinguishable; authority, scope, and request generations abort and discard older evidence, and unresolved refresh generations expose no baseline mutation authority. Matches uses informational rather than compliance-success treatment, differs uses warning rather than policy-drift treatment, provider text remains inert, and neither state is labelled compliant, protected, drift-free, enforcing, release-ready, or safe for CI omission.",
      "claimLevel": "blocking",
      "riskClass": "critical",
      "proofBindingRefs": ["proofkit/requirement-bindings.json"],
      "nonClaimRefs": ["NC-CI-UI-013"],
      "nonClaims": ["This requirement does not prove backend authorization, provider freshness beyond the named observation, snapshot isolation, persistence correctness, policy intent, compliance, policy-classified drift, continuous monitoring, provider enforcement, remediation authority, release readiness, CI omission safety, deployment, or production readiness."],
      "lifecycle": {"state": "active", "replacementRequirementIds": [], "evidenceRefs": []},
      "deferral": null,
      "updatePolicy": {"reviewOwnerId": "ci-coordinator.operator-ui", "requiresImpactDeclaration": true, "requiresProofBindingReview": true}
    },
    {
      "requirementId": "REQ-CI-UI-014",
      "ownerId": "ci-coordinator.operator-ui",
      "invariant": "The production frontend bundle contains one canonical sorted manifest for every served regular asset with exact canonical relative path, media type, byte count, and SHA-256. Before materializing any asset payload, build admission and backend composition independently pin the bundle-root descriptor and every bundle-internal ancestor through descriptor-relative no-follow traversal, prove the exact filesystem path and size inventory, each per-file bound, and the aggregate 32 MiB bundle bound, reject symlinks, special files, missing, duplicate, extra, unsupported, unreadable, oversized, digest-mismatched, or mutable entries, and require an unchanged final inventory. Composition reads each admitted asset once at its declared exact size into an immutable serving snapshot; an unversioned exact member is a no-store redirect and only the corresponding exact member under the SHA-256 manifest-digest namespace receives one-year immutable caching. GET and HEAD retain identical representation metadata without a HEAD body, weak and wildcard validators across every repeated If-None-Match field line use conditional-request semantics, the shell remains no-store, and the manifest is not served. Browser fetch timeout input is admitted centrally as a finite positive safe integer within the platform limit before network access, every public client uses one result algebra, and every caller-selected response bound is at most the 32 MiB platform maximum. For an unencoded response, a supplied Content-Length must be canonical and equal the decoded body length; malformed, duplicate-shaped, oversized, short, or long declarations are rejected. Encoded responses use the bounded no-length path, and a materialized decoded response removes obsolete Content-Encoding and Content-Length metadata. Stream cancellation and reader release are best-effort cleanup and cannot delay or replace an already classified outcome. Timeout remains network failure; malformed or cross-scope data remains invalid response.",
      "claimLevel": "blocking",
      "riskClass": "critical",
      "proofBindingRefs": ["proofkit/requirement-bindings.json"],
      "nonClaimRefs": ["NC-CI-UI-014"],
      "nonClaims": [
        "This requirement does not prove external CDN integrity, browser SRI enforcement, build provenance, zero-copy response parsing, or live browser memory capacity."
      ],
      "lifecycle": {"state": "active", "replacementRequirementIds": [], "evidenceRefs": []},
      "deferral": null,
      "updatePolicy": {"reviewOwnerId": "ci-coordinator.operator-ui", "requiresImpactDeclaration": true, "requiresProofBindingReview": true}
    },
    {
      "requirementId": "REQ-CI-UI-015",
      "ownerId": "ci-coordinator.operator-ui",
      "invariant": "Frontend evidence includes blocking V8 coverage with explicit generated and declarative exclusions and per-owner floors for risk-bearing admission, identity, transport, and state-transition code, plus a bounded mutation witness over those same kernels. The frontend mutation baseline requires bounded machine-readable Vitest evidence proving that at least one test executed and passed; a mutant is killed only by bounded internally consistent Vitest evidence proving an executed assertion failure together with the admitted exit status. Collection, configuration, no-test, unhandled-only, missing-report, oversized-report, and contradictory-counter failures are invalid evidence rather than killed mutants. Every frontend mutation operator must represent a named dangerous counterexample, surviving mutants remain unresolved, and no whole-repository percentage or rendering-boilerplate mutation is treated as proof of test adequacy. Chromium remains the sole blocking browser-support contract until a separately accepted product decision adds another engine.",
      "claimLevel": "blocking",
      "riskClass": "high",
      "proofBindingRefs": ["proofkit/requirement-bindings.json"],
      "nonClaimRefs": ["NC-CI-UI-015"],
      "nonClaims": [
        "Coverage and mutation evidence do not prove complete correctness, production browser capacity, Firefox or WebKit support, deployment, or production readiness."
      ],
      "lifecycle": {"state": "active", "replacementRequirementIds": [], "evidenceRefs": []},
      "deferral": null,
      "updatePolicy": {"reviewOwnerId": "ci-coordinator.operator-ui", "requiresImpactDeclaration": true, "requiresProofBindingReview": true}
    },
    {
      "requirementId": "REQ-CI-UI-016",
      "ownerId": "ci-coordinator.operator-ui",
      "invariant": "The operator console separates the organization repository catalog from a single visible repository task: overview, workflow discovery and activation, CI economics, governance, or audit. Overview evidence has one accessible selected tab without dropping the other reachable tables or their failure and truncation states. Closed same-origin view/tab query coordinates preserve the existing scope admission, reload, modifier-click and browser back/forward semantics; duplicate or invalid scope coordinates cannot issue scoped evidence or command requests, while independently authorized catalog reads remain available. Navigation never grants authority, submits a command or manufactures a new retry identity. Unvisited expensive task views are not mounted; visited workflow and governance form state survives same-scope view navigation but is discarded on repository or session-authority change, with existing cancellation and stale-response rejection preserved. Catalog search and pagination survive return navigation. A provider-derived repository label is display-only and cannot follow a different repository or session authority. Inactive tasks are absent from layout, accessibility exposure and focus order; primary links identify the current page, evidence tabs implement arrow/Home/End navigation, mobile navigation retains readable names and touch targets, and task changes announce a heading with visible focus. Exact repository-review callback hints continue to require backend replay and are not authority. The checked Chromium desktop, touch and narrow-reflow journeys must demonstrate these relations in the production shell. The sidebar is retained on all views. Above the mobile breakpoint it defaults to compact on the portfolio and expanded inside a repository unless the user explicitly chooses either mode for the current page lifetime. Compact presentation preserves named keyboard-reachable icon links and current-page state; the independent mobile disclosure retains visible labels. Changing sidebar presentation preserves route, scope, mounted drafts and request identities. The labelled native Organization select has no overlapping decorative icon. Closed economicsTab and activitySource coordinates bind the active view and admitted scope; defaults are omitted, wrong-view or duplicate values are discarded during normal navigation and rejected by strict SSO canonical equality. Same-scope primary navigation may retain inactive tab selection in memory, but scope replacement resets it and URLs serialize only the active subview.",
      "claimLevel": "blocking",
      "riskClass": "high",
      "proofBindingRefs": ["proofkit/requirement-bindings.json"],
      "nonClaimRefs": ["NC-CI-UI-016"],
      "nonClaims": ["Navigation does not add backend configuration policy, provider permissions, dispatch or omission authority; it does not prove production readiness or untested browser support."],
      "lifecycle": {"state": "active", "replacementRequirementIds": [], "evidenceRefs": []},
      "deferral": null,
      "updatePolicy": {"reviewOwnerId": "ci-coordinator.operator-ui", "requiresImpactDeclaration": true, "requiresProofBindingReview": true}
    },
    {
      "requirementId": "REQ-CI-UI-017",
      "ownerId": "ci-coordinator.operator-ui",
      "invariant": "The repository economics console separates registered provider runs, explicit bounded discovery and existing reconciled attempts into accessible tabs. Retained-source and report-pointer pagination admits exact requested repository and source identities, safe cursors, strict order, finite page sizes and bounded responses; a continuation cannot skip the declared page limit. Source discovery is an explicit audit-authorized CSRF-bound POST; registration is a separate configure-authorized command bound to the displayed attempt, and replay, conflict, capacity or retention refusals remain distinct. Navigation never registers, dispatches or changes CI. Repository or session-authority changes discard selection, abort obsolete requests and reject late responses. Full reports are fetched on selection and must match pointer identity, digest and retention; comparison retains both selected report identities and displays only the server-projected outcome. Queue, runner occupancy, attempt wall, scoped process CPU and reporter elapsed remain distinct, with units, provenance and unknown reasons. Ad-hoc report budgets retain caller-supplied threshold and exact-report scope. No missing metric becomes zero, comparison becomes causal saving or budget becomes a persistent alert. The complete read, discovery, registration, report and comparison journeys must have native component and desktop/touch/narrow Chromium witnesses, including failure and stale-response cases. The route owns the active economicsTab. Fresh session admission may restore this inert tab selection through the existing bounded SSO hint while all old command, draft, response and token state remains discarded.",
      "claimLevel": "blocking",
      "riskClass": "high",
      "proofBindingRefs": ["proofkit/requirement-bindings.json"],
      "nonClaimRefs": ["NC-CI-UI-017"],
      "nonClaims": ["Browser admission does not establish provider authenticity, cryptographic report authority, production capacity, causal CPU savings, persistent budget policy or authority to omit CI."],
      "lifecycle": {"state": "active","replacementRequirementIds": [],"evidenceRefs": []},
      "deferral": null,
      "updatePolicy": {"reviewOwnerId": "ci-coordinator.operator-ui","requiresImpactDeclaration": true,"requiresProofBindingReview": true}
    },
    {
      "requirementId": "REQ-CI-UI-018",
      "ownerId": "ci-coordinator.operator-ui",
      "invariant": "The repository economics console provides separate keyboard-accessible budget and signal views. Configure-authorized users can create, edit and disable bounded policies using exact current revision and a fresh operation identity; audit-only users see no mutation controls. The browser validates the complete strict generated command and response, scope, operation, revision and configuration. Confirmed conflicts remain distinct from unknown writes; neither path is retried automatically or displayed as success, and further editing requires an explicit refreshed read. Scope or session-authority changes abort requests and discard obsolete editors and evidence. Signal pages preserve historical policy revision, exact canonical policy and signal digests, counter arithmetic, command failure, unavailable evidence, retention, requested filters and continuation; they do not imply current compliance or provider authority. Forms, long identifiers and signal metadata remain usable on desktop, touch and narrow viewports. These controls neither modify workflows nor change CI selection or omission.",
      "claimLevel": "blocking",
      "riskClass": "high",
      "proofBindingRefs": ["proofkit/requirement-bindings.json"],
      "nonClaimRefs": ["NC-CI-UI-018"],
      "nonClaims": ["Browser validation is not storage atomicity, producer authenticity, statistical regression, alert delivery, causal savings or production qualification."],
      "lifecycle": {"state": "active", "replacementRequirementIds": [], "evidenceRefs": []},
      "deferral": null,
      "updatePolicy": {"reviewOwnerId": "ci-coordinator.operator-ui", "requiresImpactDeclaration": true, "requiresProofBindingReview": true}
    },
    {
      "requirementId": "REQ-CI-UI-019",
      "ownerId": "ci-coordinator.operator-ui",
      "invariant": "The repository economics Observation view exposes explicit enable, pause, all-or-selected workflow filtering and bounded initial history without registering or configuring on navigation. Browser admission rebinds every scoped response, command identity, revision, configuration, scan coupling/window bound and gap identity/lifetime. Workflow choices are lazy bounded provider pages; inert metadata never becomes authority and configured IDs missing from a page remain selectable. One visibility-gated status request may run at a time; successful enabled state permits finite polling, while failures stop automatic retry and retain visibly stale evidence without overwriting dirty drafts. A newer server revision requires explicit reload before a new command. Unknown writes preserve the exact original command for explicit identical retry and cannot become success or an editable new operation. Scope or session-authority changes abort obsolete requests and discard their state. Current configure permission and CSRF remain mandatory; audit-only users cannot mutate. Progress separates claimed scans, registered sources, captured evidence and historical failures without invented percentages or zero-valued missing metrics. Gap details load only when requested. The development proxy admits only exact supported methods, paths and bounded query forms. Native schema, component and production-bundle Chromium journeys cover uncertain outcomes, conflicts, refresh, scope changes, responsive layout and accessibility.",
      "claimLevel": "blocking",
      "riskClass": "high",
      "proofBindingRefs": ["proofkit/requirement-bindings.json"],
      "nonClaimRefs": ["NC-CI-UI-019"],
      "nonClaims": ["Browser state and fixture journeys do not prove durable commit, current provider availability, complete history, CPU savings, production qualification or permission to change CI selection."],
      "lifecycle": {"state": "active", "replacementRequirementIds": [], "evidenceRefs": []},
      "deferral": null,
      "updatePolicy": {"reviewOwnerId": "ci-coordinator.operator-ui", "requiresImpactDeclaration": true, "requiresProofBindingReview": true}
    },
    {
      "requirementId": "REQ-CI-UI-020",
      "ownerId": "ci-coordinator.operator-ui",
      "invariant": "The repository History view exposes explicit initial UTC range, all-or-selected workflows, enable/pause/rescan, retention inheritance or an exact override and bounded editable quotas without configuring on navigation. Generated types and strict runtime schemas bind every response to scope, command identity, revision, configuration, retention source and closed outcome; malformed input and contradictory snapshots cannot be shown as saved. One visible-only status poll owns its cancellation and at most one queued refresh; failure stops automatic polling and retains visibly stale data. Dirty drafts cannot be overwritten or silently rebased. Unknown mutations freeze the original command for explicit identical retry and are never automatically replayed after authentication recovery. Scope and session authority replacement abort old work and discard its state. Progress exposes only recorded counters, explicit gaps and frozen intervals; completed traversal does not imply complete provider history, expiry does not imply a running worker, and only a real fetch animates with reduced-motion support. Configure permission, CSRF and exact development proxy routes remain mandatory. Native client, form, polling and production-bundle browser witnesses cover failure, retry, scope changes, keyboard, accessible state and responsive layout.",
      "claimLevel": "blocking",
      "riskClass": "high",
      "proofBindingRefs": ["proofkit/requirement-bindings.json"],
      "nonClaimRefs": ["NC-CI-UI-020"],
      "nonClaims": ["Frontend admission and browser fixtures do not prove durable storage, provider completeness, gap-free traversal, measured capacity, live authentication, production qualification or CI omission authority."],
      "lifecycle": {"state": "active", "replacementRequirementIds": [], "evidenceRefs": []},
      "deferral": null,
      "updatePolicy": {"reviewOwnerId": "ci-coordinator.operator-ui", "requiresImpactDeclaration": true, "requiresProofBindingReview": true}
    },
    {
      "requirementId": "REQ-CI-UI-021",
      "ownerId": "ci-coordinator.operator-ui",
      "invariant": "Historical records, exact-attempt jobs, gap observations and collection settings are separate bounded views. Generated types and strict runtime admission bind returned scope, generation, filters, unique ordered populations, detail metadata and accounting. Pagination is query-bound; stale continuation restarts from a fresh first page without inventing coverage. Retention preview never mutates; configure-authorized confirmation uses the reviewed selection and digest, current CSRF and one operation identity. An uncertain result retains its exact command across same-authority navigation for explicit identical retry. Ordinary detail deletion cannot erase statistics. Scope/session-authority changes cancel obsolete work; inactive archive reads do not continue. Optional detail content and upstream availability are labelled honestly. Account controls remain accessible in the sidebar on expanded, compact and mobile layouts.",
      "claimLevel": "blocking",
      "riskClass": "high",
      "proofBindingRefs": ["proofkit/requirement-bindings.json"],
      "nonClaimRefs": ["NC-CI-UI-021"],
      "nonClaims": ["Browser witnesses do not prove database atomicity, complete history, mutation commit after a lost reply, live login or production qualification."],
      "lifecycle": {"state": "active", "replacementRequirementIds": [], "evidenceRefs": []},
      "deferral": null,
      "updatePolicy": {"reviewOwnerId": "ci-coordinator.operator-ui", "requiresImpactDeclaration": true, "requiresProofBindingReview": true}
    },
    {
      "requirementId": "REQ-CI-UI-022",
      "ownerId": "ci-coordinator.operator-ui",
      "invariant": "Historical analytics presents exact period/workflow/job selections, measured runner usage and job outcomes with explicit sample support and unknown provider completeness. Charts use a fixed finite coordinate space, distinguish observations from forecasts, break lines across unknown values and provide a bounded semantic table. Forecast refusal, empirical intervals and unclassified slowdown remain visible without claims of CPU, financial savings or causality. Strict response admission rebinds query, bucket partitions, cutoff, units and support; obsolete requests are cancelled and late foreign responses cannot render. Development proxy capabilities remain a closed method/path/query projection. Native component and production-bundle browser witnesses cover sparse data, invalid geometry, filters, inaccessible evidence, responsive layout and accessibility.",
      "claimLevel": "blocking",
      "riskClass": "high",
      "proofBindingRefs": ["proofkit/requirement-bindings.json"],
      "nonClaimRefs": ["NC-CI-UI-022"],
      "nonClaims": ["A rendered chart is not provider completeness, statistical causality, realized cost saving, prospective forecast qualification or production readiness."],
      "lifecycle": {"state": "active", "replacementRequirementIds": [], "evidenceRefs": []},
      "deferral": null,
      "updatePolicy": {"reviewOwnerId": "ci-coordinator.operator-ui", "requiresImpactDeclaration": true, "requiresProofBindingReview": true}
    },
    {
      "requirementId": "REQ-CI-UI-023",
      "ownerId": "ci-coordinator.operator-ui",
      "invariant": "The global administrator Activity view exposes issuer-scoped access events without requiring a selected repository or manual issuer entry, and a separate exact-repository business-action view when a scope is selected. Finite UTC filters, action and actor selection, signed-cursor pagination and explicit bounded JSON export use the existing Activity API and shared session-aware transport. Wire admission verifies source context, row limits, sequence order, action/outcome algebra, timestamp boundaries and audit reference identity before display or download. Scope, source, filter and authority replacement cancels obsolete work; exports never retry automatically. Existing audit evidence remains distinct; the UI presents retention and projection limitations and remains keyboard-accessible and responsive. The route owns security or business source selection; only scoped business selection is serialized as activitySource. Renewal restores the source but not actor/filter/cursor/export state. Human event times use the shared UTC formatter while the original instant remains in the time element.",
      "claimLevel": "blocking",
      "riskClass": "high",
      "proofBindingRefs": ["proofkit/requirement-bindings.json"],
      "nonClaimRefs": ["NC-CI-UI-023"],
      "nonClaims": ["The Activity interface does not prove all-actions coverage, business-chain integrity, export delivery, IdP logging, deployment authorization or production readiness."],
      "lifecycle": {"state": "active", "replacementRequirementIds": [], "evidenceRefs": []},
      "deferral": null,
      "updatePolicy": {"reviewOwnerId": "ci-coordinator.operator-ui", "requiresImpactDeclaration": true, "requiresProofBindingReview": true}
    },
    {
      "requirementId": "REQ-CI-UI-024",
      "ownerId": "ci-coordinator.operator-ui",
      "invariant": "Repository Configuration projects the existing versioned configuration lifecycle API without owning policy. JSON/YAML upload and editing preserve bounded source bytes; validation binds exact source, format and selected repository before registration. Retained epoch status and source export re-admit scope, epoch, hash and byte identity. Rollback selects a retained target, current revision and explicit reason before confirmation. An uncertain registration or rollback preserves the exact command and operation ID through same-scope/authority tab and primary navigation, without automatic replay or replacing reviewed bytes. Hidden reads stop; obsolete scope or authority clears foreign state. Registration never creates activation authority: the UI hands off to the existing attested workflow-proposal path. Native wire, state, source, navigation and browser witnesses cover each independent operand and accessibility.",
      "claimLevel": "blocking",
      "riskClass": "critical",
      "proofBindingRefs": ["proofkit/requirement-bindings.json"],
      "nonClaimRefs": ["NC-CI-UI-024"],
      "nonClaims": ["This view does not authorize arbitrary policy activation, provider workflow mutation, full API parity outside configuration, persisted browser drafts, deployment, source authenticity beyond the admitted service or production readiness."],
      "lifecycle": {"state": "active", "replacementRequirementIds": [], "evidenceRefs": []},
      "deferral": null,
      "updatePolicy": {"reviewOwnerId": "ci-coordinator.operator-ui", "requiresImpactDeclaration": true, "requiresProofBindingReview": true}
    },
    {
      "requirementId": "REQ-CI-UI-025",
      "ownerId": "ci-coordinator.operator-ui",
      "invariant": "Analytics offers bounded explicit purpose mapping rows through the repository-scoped API, distinguishing missing from configured-empty settings. A reviewed save binds generation/revision, one operation and exact entries; unknown outcome retains the command across same-authority navigation without automatic replay or replacement. Scope/session replacement cancels obsolete reads and clears foreign commands; refresh cannot overwrite an edited draft. Responses rebind every request coordinate. Categories remain descriptive filters with unknown and mixed populations visible, not omission evidence. Native component and browser scenarios cover editing, replay, substitution, accessible controls and responsive layout.",
      "claimLevel": "blocking",
      "riskClass": "high",
      "proofBindingRefs": ["proofkit/requirement-bindings.json"],
      "nonClaimRefs": ["NC-CI-UI-025"],
      "nonClaims": ["The editor does not prove database atomicity, automatic workflow classification, deployment or completed production onboarding."],
      "lifecycle": {"state": "active", "replacementRequirementIds": [], "evidenceRefs": []},
      "deferral": null,
      "updatePolicy": {"reviewOwnerId": "ci-coordinator.operator-ui", "requiresImpactDeclaration": true, "requiresProofBindingReview": true}
    }
  ],
  "nonClaims": [
    "This requirement source does not independently prove backend identity, repository authorization, mutation atomicity, provider availability, deployment approval, or production readiness."
  ]
}
