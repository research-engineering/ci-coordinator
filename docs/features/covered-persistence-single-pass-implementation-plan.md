# Covered Persistence Single Pass Implementation Plan

Design: [Covered Persistence Single Pass](covered-persistence-single-pass.md).
Owner: repository verification.

1. Keep the existing coverage and targeted persistence implementations,
   application/persistence tests, database fixtures, provider image, source
   denominator and thresholds unchanged.
2. Remove only the second persistence execution from the Full Check PostgreSQL
   job. Preserve the aggregate quality plan's distinct environment observations,
   standalone and selective commands,
   stable required jobs, mutation cohorts and process deadlines.
3. Extend the existing command-bound witness to assert complete unfiltered
   coverage selection through the actual command and public dispatcher, and
   unchanged targeted persistence selection. Verify the workflow uses that exact
   command while the aggregate retains both existing environment envelopes.
   Update the existing workflow owner's ordering and failure tests rather than
   duplicating its parser. Preserve required failure propagation and document
   removal of the separate early persistence-phase result.
   Close the complete run-step inventory, pytest discovery configuration,
   environment/shell override inputs and the unchanged 55-minute job deadline.
4. Bind the two new documents to the existing proof requirements. Recompute
   workflow discovery digests in both canonical and packaged inventories;
   update navigation without rewriting predecessor design documents.
5. Run admitted static gates locally. Obtain bounded independent review and
   exact-head GitHub Full Check, including real PostgreSQL behavior, before
   squash merge. Compare native step/job times against the baseline, keeping
   measured elapsed time distinct from CPU and unproved statistical savings.

Acceptance is unchanged oracle coverage with one GitHub job persistence
execution, not a smaller test count, weaker coverage floor, larger timeout or
new skip. If selection containment or fixture independence is falsified, retain
the original aggregate until an owner-valid replacement is established.
