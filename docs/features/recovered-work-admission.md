# Recovered Work Admission

Status: migration dispositions; unresolved prototypes do not grant runtime authority.

## Scope

Evaluate recovered local work against current contracts rather than replaying
old files over newer implementations. Existing native and provider qualification
remains required. The private migration inventory preserves exact preimages;
historical material is not public-source acceptance evidence.

## Retained Repairs

- Workflow discovery admits bounded opaque scalar values without expanding
  mapping-key or externally projected field limits. Scripts remain opaque and
  never enter discovery evidence. File, node and depth budgets still apply.
- Deterministic-plan and omission-proof mismatches retain distinct bounded
  operational metric labels; metrics cannot change planning decisions.
- Audit replay classifies persisted-record invariant failures as invalid
  evidence (exit 4), not unavailable storage (exit 2). Paged replay, cleanup,
  redaction and publication only after complete staging remain intact.
- Audit codec tests retain valid values at exact maximum text/payload sizes,
  complementing existing rejection tests above those bounds.
- Optional local IDE inspection setup complements native checks. A parsed
  configuration is not a qualified Qodana execution or a new required CI gate.

## Unfinished Candidates And Delivery Order

| Candidate | Existing batch | Required admission before implementation |
| --- | --- | --- |
| Review of an already registered policy epoch | B3 API-first adoption; prerequisite assessment in B1 own-CI pilot | Prove that the journey is needed beyond current discovery proposals. Bind actor, scope, provider revision, exact epoch, baseline, one-use handoff, audit identity and activation freshness. Complete HTTP/runtime wiring, forward migration and independent replay/race tests together. |
| Structured decision provenance in reconciliation evidence | B5 selective CI and B7 audit closure | Compare the proposed policy/decision/provenance relation with current codecs and evidence owners. Admit complete identity, canonical bytes, retained-data migration, replay and verifier tamper oracles. Do not remove current policy rules or default-branch semantics. |
| Critical runtime mutation cohort and owner-class coverage | B1 own-CI assurance and B7 audit closure | Rebind unknown-path fallback, omission-proof independence, JWKS expiry and numeric repository identity mutants to current witnesses. Demonstrate meaningful kills and compare current tuple/risk gates before adding another coverage mechanism. |
| Additional workflow identifier grammar | B3 whole-workflow discovery | Establish a current supported-provider grammar and a reachable workflow first; preserve expression/path boundaries. An old parser relaxation alone does not justify a new accepted language. |

These are preserved candidates, not implemented capabilities or automatically
accepted designs. Keep the original code privately outside the repository until
adjudication. Implement only the smallest missing behavior, with a focused
design, preserved observables, falsifiers and current native qualification.
Reject a candidate when a current owner already provides an equivalent result.

## Superseded Work

Do not restore token-bearing browser sessions over the current separated
control-plane and repository-review identities, old read-model session fences
over the coherent statement snapshot, an old dependency lock, or draft
developer-experience documents over their integrated successors. Transport
ownership, generator noninterference and budget-policy behavior are already
represented by current source and native witnesses.
