# Assurance Audit Hardening Implementation Plan

Status: implementation plan
Date: 2026-09-23

Owner rationale and the complete grouped audit disposition are in the
[design](assurance-audit-hardening.md). Global status remains in
[ROADMAP](../../ROADMAP.md); this batch advances B7 and removes B1 qualification
friction without replacing the existing eight-batch plan.

## Ordered Delivery

1. Bind report hash, source/base and new GitHub repository identity. Preserve
   all user files and the unpublished operator plan. Work in a new branch.
2. Extend runtime mypy coverage and repair its narrow annotations. Update
   route/command witnesses and generated inventories together.
3. Repair the two descendant timeout witnesses with an observed setup barrier
   and parent-only failure controls. Add the independently expected valid and
   invalid policy lane to the API contract campaign.
4. Wire immutable packaged build identity into structured logs, including
   fallback/truncation; keep redaction, cardinality and business behavior.
   Reuse integer formatting in observation UI and add a regression witness.
5. Repair qualification event/cache/environment declarations and add the
   focused embedded-timezone witness. Keep release policy and image pins.
6. Add current dependency/evidence clarification; route every deferred report
   item to the existing operational or risk-based assurance workstream.
7. Run native lightweight checks locally, freeze the candidate for one review
   under AGENTS policy, adjudicate findings, then retain the exact diff and
   unresolved remote-test gate. No local behavioral substitute.
8. After runner recovery, execute exact-head targeted/full GitHub qualification,
   then normal review/merge/release. A green static plan is not runtime proof.

## Acceptance Matrix

| Changed guarantee | Independent falsifier | Required evidence |
| --- | --- | --- |
| Runtime Python scope | A docker/runtime annotation error escapes mypy or private probes are broadly ignored | Exact argv/source routes; strict mypy; command-selection tests |
| Descendant timeout | No child existed, or only the parent was killed | Handshake witnessed before deadline; positive and parent-only negative controls with bounded cleanup |
| API semantic examples | Invalid policy admitted by a shared faulty classifier still passes | Generated valid policy with literal expected scope/identity and invalid contrast; role/effect guards unchanged |
| Build diagnostics | Caller overwrites source/release or fallback loses identity | Constructor-bound fields, missing-identity behavior, redaction/truncation and composition tests |
| UI numbers | Ambient locale changes observation counters | Existing formatter plus non-default-locale regression |
| Qualification route | Only old branch can qualify or refs share write cache scope | PR/master/manual triggers, exact ref cache relation, read-only token, native workflow lint |
| Embedded timezone | Embedded import is removed, or a valid non-UTC zone fails | AST import guard plus valid/invalid zone controls; no host-independence claim from lookup alone |
| Documentation | Present-tense dependency rationale points only to old versions | New current source-bound clarification and reachable links; historical payload unchanged |

## Remaining Evidence

### Portable Follow-Up Order

1. Preserve the source merge receipt and account-level blockers separately.
2. Restore the operator completion plan and its roadmap/index/proof routes.
   Retain final clean-history publication after implementation and qualification.
3. Separate provider dependency review from native quality without changing the
   final PR gate. Exercise success, failure and inapplicable event outcomes.
4. Replace the obsolete performance baseline with exact current-repository
   source and isolate the layer-reuse mutation in a disposable snapshot. Test
   failed setup/build, source drift and absent/manual/PR/push baseline choices.
5. Adjudicate the connected browser diagnosis and implement only the confirmed
   minimal repair with a causal regression witness, retaining all SQL/effect
   and authentication controls.
6. Run affected local tests under the user's focused-test authorization, then
   independent frozen review and exact-head GitHub qualification. Report any
   remaining private-plan provider limitation without converting it to pass.

The runner blocker is external and explicit. Preserve C3 delivered-userspace
tests, L1-L3/L9 current performance qualification, L6 reviewed repair renewal,
L7/L8 runtime hardening, IV-04/05 analyzer/cost provenance, IV-12-14 database
semantics/security/restore and remaining clause/SARIF occurrence review in B7.
These are not marked fixed by this batch. Source completion, native CI, release,
deployment and live product acceptance are separate states.

### Companion Audit Revalidation

The 2026-09-23 companion intake was checked against source `c5a9be3`, not the
historical audit epoch. The named source repairs for A9, d2/IV-03, A3/A4, J11,
IV-08/09/11, R9-R11 and L7 are present. Qualify the existing matrix on the new
repository identity instead of implementing those repairs again. Source
inspection does not supply native or production acceptance.

PR207 R1 and R6 also have source successors: native Go admission rejects
`CgoFiles`, `IgnoredGoFiles` and `InvalidGoFiles`, and self-CI admission checks
current Git paths/modes before graph-based omission. Qualify the cgo,
mode-only and deleted-path/stale-graph controls on the exact new source;
do not reimplement the guards. Assigned commands, actually consumed inputs and
applicable properties remain distinct. Scheduled workflow presence alone does
not prove a delivered-artifact advisory scan.

| Existing follow-up | Concrete closure criterion |
| --- | --- |
| C3 delivered userspace | Declare a bounded image-compatible pytest subset and exclusions; bind image digest, actual interpreter/libc/native dependencies and the injected test harness. Prove execution did not use host or substituted runtime libraries. Smoke is separate; do not add test dependencies to the production image. |
| L8 library inventory | Define scan roots and resolved-copy identity; test versioned zlib files, symlink aliases, a real unversioned `libz.so` and an independent `/usr/lib64`. Avoid omissions and double counting. Static or renamed copies remain outside a named-library claim unless separately covered. |
| L10 admission environment | Preserve imports from the admitted source root and identical rejection of invalid repair evidence under `PYTHONOPTIMIZE` and `PYTHONPATH` variations. Adding `-I` without a working import contract is not closure; no observed exploit is claimed. |
| IV-04 analyzer provenance | Bind the analyzer actually executed and the database snapshot actually read. A version-command timestamp is not the scan snapshot. If exact binding is unavailable, expose that reproducibility limit rather than inventing an epoch. |
| IV-05 cost hints | Retain measurement source/run/artifact identity or explicitly classify foreign/stale hints as heuristics with a defined fallback. Hints may affect balancing, never the complete disjoint test universe, required outcomes or omission authority. A measurement need not equal every future HEAD unless its owner requires that. |
| IV-12/13/14 database qualification | Distinguish fresh and reused databases; attest relevant text semantics with an owner-specific comparison witness. Choose threat-bound credential transport preserving cleanup and excluding secrets from forbidden argv/log/environment surfaces. Exercise admitted restore with independent state/ACL/integrity checks and RPO/RTO; a CLI or artificial downgrade is not restore evidence. Link B2 retention/erasure. |
| IV-01/02 compatibility choices | Before changing duplicate-key or redirect behavior, admit method/body/auth/target scope and the user-visible compatibility delta. Distinguish envelope from nested documents. Adopt a scoped policy with negative witnesses or document the absence of a general requirement; neither report establishes a blanket vulnerability. |
| Remaining SARIF/clauses | Bind each accepted occurrence to report hash, stable ID and current owner/source. Retain overlapping cohorts and unresolved dispositions explicitly. Historical counts and the 382 unverified generic clauses are not new defects or blanket N/A; exhaustive qualification needs its own admitted scope. |

The historical occurrence input SARIF
`3ed45d384dd107b6c5109480984d241f6f68b3bc0d1ab27784f9ff59cc0b8634`
for source `065a4d9c` is retired-unadjudicated by the owner's 2026-09-23 decision;
its raw file need not be retained. Already adopted predicates and remediation
criteria remain owned here; retiring the input does not classify its unresolved
occurrences as false positives or passed. Future occurrence review must use a
fresh report bound to the current source, analyzer configuration and version.
Do not block current delivery on reconstructing this obsolete report.

Do not conflate it with report
`ca764736b74ee18befe9a3956dbaf8d05bae4d1a9e64bbadf6b050bc9565405a`.
Its 81 High non-Python rows have complete scoped dispositions in the
[Qodana adjudication](../adoption/qodana-high-adjudication-2026-09-20.md), so that
task no longer needs a raw archive. Its excluded 944 Python findings and other
severities are not declared adjudicated, superseded or automatically assigned
to a new review. Any extension must name the relevant input and population.

### Legacy Input Conservation

Two compact primary inputs still have predicates requiring current-owner
adjudication: the July stack ledger (147 STK and 13 CONT rows) and the July
engineering review (78 candidates). Keep their exact originals only until
each unresolved predicate and necessary falsifier is conserved in an existing
owner or explicitly retired with a reason. Their old severities, TypeScript
migration phases and execution statuses are not a current defect backlog.
Do not replay the old delivery sequence, restore deleted TypeScript owners or
equate a generic B7 link with conservation of an exact counterexample.

Two current source observations already justify specific follow-up:

| Input | Current observation | Existing owner and smallest next step |
| --- | --- | --- |
| July M-18 | Runtime settings admit plan TTL up to 3600 seconds and composition passes that value to the signer; the delivered target validator accepts at most 300. Without a tighter `not_after`, including unbounded-by-authority fallback signing, TTL 301 produces an envelope the consumer rejects. A tighter authority expiry can shorten a particular envelope; it does not make the configuration domains equal. | B7 signing/target contract, relevant to B1 configured execution. Bind one admitted producer/consumer lifetime intersection and fail at configuration admission when incompatible; preserve the target security window unless an explicit policy change is accepted. Qualify 1, 300, 301 and over-limit values, with and without an authority clamp, through actual settings, signing and target validation. |
| July H-22 | `ReconciliationPublisher` has only its declaration and package exports in the current repository, with no current implementation or caller. | B7 abstraction cleanup. Remove the unconsumed public port if no admitted current consumer or external compatibility obligation exists; do not invent provider-writing behavior merely to use the interface. Preserve the actual reconciliation read/decision boundaries. |

Other legacy labels remain hypotheses until their owners and applicability
are bound. Keep shared-source inputs separate from this project-owned review.
This is bounded intake within B7, not 238 new confirmed defects or another
global roadmap. Deletion of the originals requires an explicit conservation
result, not an assumption that a newer codebase fixed every old concern.

These criteria refine B7 and its existing dependencies, not another audit
program. Re-read current provider blockers before execution; historical account
limitations do not establish that the new target is blocked. Changed source,
owner policy or a new counterexample invalidates the corresponding disposition.

Repository transfer additionally requires App scope/webhook, trusted OIDC and
attestation repository, GHCR ownership, secrets/rulesets and inventory identity
readback. Historical evidence keeps its original owner and subject. Never do
a global old-owner string replacement that would rewrite historical proof or
silently grant a new authority.
