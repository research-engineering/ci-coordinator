# Assurance Audit Hardening Implementation Plan

Status: implementation plan
Date: 2026-09-23

Owner rationale and the complete grouped audit disposition are in the
[design](assurance-audit-hardening.md). Global status remains in
[ROADMAP](../../ROADMAP.md); this batch advances B7 and removes B1 qualification
friction without replacing the existing eight-batch plan.

## Ordered Delivery

1. Bind report hash, source/base and new GitHub repository identity. Preserve
   all user files and the unpublished operator plan. Work in a new branch.
2. Extend runtime mypy coverage and repair its narrow annotations. Update
   route/command witnesses and generated inventories together.
3. Repair the two descendant timeout witnesses with an observed setup barrier
   and parent-only failure controls. Add the independently expected valid and
   invalid policy lane to the API contract campaign.
4. Wire immutable packaged build identity into structured logs, including
   fallback/truncation; keep redaction, cardinality and business behavior.
   Reuse integer formatting in observation UI and add a regression witness.
5. Repair qualification event/cache/environment declarations and add the
   focused embedded-timezone witness. Keep release policy and image pins.
6. Add current dependency/evidence clarification; route every deferred report
   item to the existing operational or risk-based assurance workstream.
7. Run native lightweight checks locally, freeze the candidate for one review
   under AGENTS policy, adjudicate findings, then retain the exact diff and
   unresolved remote-test gate. No local behavioral substitute.
8. After runner recovery, execute exact-head targeted/full GitHub qualification,
   then normal review/merge/release. A green static plan is not runtime proof.

## Acceptance Matrix

| Changed guarantee | Independent falsifier | Required evidence |
| --- | --- | --- |
| Runtime Python scope | A docker/runtime annotation error escapes mypy or private probes are broadly ignored | Exact argv/source routes; strict mypy; command-selection tests |
| Descendant timeout | No child existed, or only the parent was killed | Handshake witnessed before deadline; positive and parent-only negative controls with bounded cleanup |
| API semantic examples | Invalid policy admitted by a shared faulty classifier still passes | Generated valid policy with literal expected scope/identity and invalid contrast; role/effect guards unchanged |
| Build diagnostics | Caller overwrites source/release or fallback loses identity | Constructor-bound fields, missing-identity behavior, redaction/truncation and composition tests |
| UI numbers | Ambient locale changes observation counters | Existing formatter plus non-default-locale regression |
| Qualification route | Only old branch can qualify or refs share write cache scope | PR/master/manual triggers, exact ref cache relation, read-only token, native workflow lint |
| Embedded timezone | Embedded import is removed, or a valid non-UTC zone fails | AST import guard plus valid/invalid zone controls; no host-independence claim from lookup alone |
| Documentation | Present-tense dependency rationale points only to old versions | New current source-bound clarification and reachable links; historical payload unchanged |

## Remaining Evidence

### Portable Follow-Up Order

1. Preserve the source merge receipt and account-level blockers separately.
2. Restore the operator completion plan and its roadmap/index/proof routes.
   Retain final clean-history publication after implementation and qualification.
3. Separate provider dependency review from native quality without changing the
   final PR gate. Exercise success, failure and inapplicable event outcomes.
4. Replace the obsolete performance baseline with exact current-repository
   source and isolate the layer-reuse mutation in a disposable snapshot. Test
   failed setup/build, source drift and absent/manual/PR/push baseline choices.
5. Adjudicate the connected browser diagnosis and implement only the confirmed
   minimal repair with a causal regression witness, retaining all SQL/effect
   and authentication controls.
6. Run affected local tests under the user's focused-test authorization, then
   independent frozen review and exact-head GitHub qualification. Report any
   remaining private-plan provider limitation without converting it to pass.

The runner blocker is external and explicit. Preserve C3 delivered-userspace
tests, L1-L3/L9 current performance qualification, L6 reviewed repair renewal,
L7/L8 runtime hardening, IV-04/05 analyzer/cost provenance, IV-12-14 database
semantics/security/restore and remaining clause/SARIF occurrence review in B7.
These are not marked fixed by this batch. Source completion, native CI, release,
deployment and live product acceptance are separate states.

Repository transfer additionally requires App scope/webhook, trusted OIDC and
attestation repository, GHCR ownership, secrets/rulesets and inventory identity
readback. Historical evidence keeps its original owner and subject. Never do
a global old-owner string replacement that would rewrite historical proof or
silently grant a new authority.
