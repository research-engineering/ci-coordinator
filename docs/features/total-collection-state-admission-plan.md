# Total Collection-State Admission Plan

The [design](total-collection-state-admission.md) owns the refinement and rollout.

1. Preserve published revisions; add the contract/expand pair and independent
   v4 catalog. Retain all unrelated capability IDs and descriptor hashes.
2. Require v4 at economics/webhook operation admission. Synchronize SQLAlchemy
   metadata, both machine profiles, loader digest and exact container inventory.
3. Prove all six states, both expired outcomes and legitimate optional fields
   survive SQL insertion and upgrade without changed row bytes. Mutate each
   nullable lease component and terminal outcome independently before SQL.
4. Prove invalid retained rows and injected final attestation failure roll back
   both revisions, constraints and declaration history. Prove intermediate-head
   rejection and predecessor/new capability behavior without exposing repositories.
5. Preserve the existing whole-upgrade exclusive-fence and bounded timeout
   witnesses; add a migration-specific held-fence rejection/rollback witness.
6. Run allowed static checks and exact Proofkit routing, then one current-policy
   independent review and native GitHub qualification. Publish repairs together;
   never weaken an oracle, rewrite published history or run local behavioral tests.
7. Squash-merge only after exact-head qualification. Release and execute the
   scoped maintenance rollout; verify readyz, authenticated pilot target and unchanged
   neighboring services. A blocked data repair or operational prerequisite is
   reported separately, not treated as completed deployment.

This closes only the confirmed NULL-admission defect and its delivery. Other
PostgreSQL catalog risks, capacity and user-interface findings remain separate.
