# Administrator Activity Workspace Plan

Review closure keeps all observer calls in an external assertion ledger, so
best-effort exception handling cannot absorb the negative oracle. The Activity
journey additionally uses two pages: the opaque cursor is admitted separately
from query filters; Next and Export must retain the exact second-page selection.
An in-flight export is resolved after source/scope replacement to prove that
the cancelled request cannot create a download. Native execution remains pending.

Design owner: [administrator activity workspace](administrator-activity-workspace.md).

1. Bind the completed backend query/context/export contract and regenerate
   OpenAPI/TypeScript through the existing owner.
2. Implement source-specific wire admission and exact request/result binding,
   preserving the existing bounded fetch and session observer.
3. Add global Activity navigation, source tabs, finite filters, rows, refresh,
   pagination and explicit JSON page export. Keep existing audit evidence intact.
4. Add positive and isolated negative native tests for scope, issuer, action,
   chronology, pagination, cancellation, stale authority and export failure.
   Cover desktop/mobile layout and keyboard interaction through GitHub browsers.
5. Register requirements, Proofkit routes and documentation links. Run static
   owners; perform one integrated independent review and exact-head native CI.
6. Squash-merge the qualified candidate, deploy its immutable image and verify
   administrator login, security/repository reads and logout on the actual Swarm target.
   Keep missing operational evidence and remaining roadmap work explicit.

## Native Qualification Repairs

The first native qualification on `5e0f368c` rejected the changed database
profile before startup because its admitted digest was stale. Bind the exact
reviewed resource bytes without weakening profile admission. Register the new
Activity service in the existing concrete-service boundary inventory.

The control review also requires nonempty actor/action filters across page two
and export, and query assertions that preserve duplicate parameter multiplicity.
Keep these as extensions of the existing witnesses, not a new harness.
Configuration source assertions select the textbox role rather than matching
both the Source tab and the field. Byte-equality witnesses compare the complete
integer sequence across host/DOM realms; no byte, hash, or metadata check is
removed. Native qualification on the repaired SHA remains required.

The newly complete service inventory exposes a concrete Activity service import
in HTTP dependencies. Use an owner-local `ActivityReadUseCase` protocol with the
same page signature and outcomes. Runtime still constructs the existing service;
authorization, persistence and effects remain unchanged. The simpler alternative
of weakening the import rule would erase a protected boundary. Mypy, the complete
boundary gate and the unchanged HTTP/service native cases validate this repair.
